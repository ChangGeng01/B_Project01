-- db/bootstrap/00_database.sql
-- 集群引导第 1 步，共 5 步。执行顺序：00 → 01 → 02 → 03 → 04，见本目录 README.md。
--
-- 【本文件在阶段 1 不含任何可执行语句】
-- 阶段 1 交付物 D-04 只交付目录与执行顺序约定，脚本内容按裁定 C-01 由阶段 2 交付，
-- 阶段 2 计划第 3.1 节是其唯一出处。在阶段 2 写入内容之前，执行本文件不产生任何数据库对象。
--
-- 本文件将承载的内容（只列归属，不在此给取值，取值以阶段 2 计划第 3.1 节为准）：
--   建库语句、库级会话默认值、库上 PUBLIC 权限的回收、public schema 的处置。
--   建库参数的排序规则取值来自阶段 1 第 13 节新增决定二，阶段 2 只执行不改其取值。
--
-- 为什么排在第 1 步：后续四步的对象要么建在本库内，要么以本库为授权范围；
-- 且建库语句须以 template0 为模板并独占一个会话，不能与其他语句共事务。
--
-- 为什么不写口令：口令由安装器从机密库读取后经 ALTER ROLE ... PASSWORD 单独注入。
-- 本目录任何文件中出现口令字面量即违反 xtask sqlcheck 规则 SQL-020。
