-- db/bootstrap/02_cluster_params.sql
-- 集群引导第 3 步，共 5 步。执行顺序：00 → 01 → 02 → 03 → 04，见本目录 README.md。
--
-- 【本文件在阶段 1 不含任何可执行语句】
-- 阶段 1 交付物 D-04 只交付目录与执行顺序约定，脚本内容按裁定 C-01 由阶段 2 交付，
-- 阶段 2 计划第 3.1 节是其唯一出处。在阶段 2 写入内容之前，执行本文件不改动任何实例参数。
--
-- 本文件将承载的内容（只列归属，不在此给任何数值）：
--   实例级参数取值，含连接数与超级用户预留、预写日志级别、复制槽与发送进程上限、
--   复制槽的本机事务日志保留上限、预加载扩展、全局语句与锁超时的兜底值。
--   这些取值来自阶段 1 第 13 节新增决定三与规格附录 A.3，阶段 2 只执行不改其取值；
--   其中复制槽保留上限一项按 R-07 需实测回填，回填由归档阶段执行。
--
-- 为什么排在 01 之后：连接数上限的分配以 01 建出的角色为对象。
-- 为什么排在 03 之前：本文件定实例级默认值，03 定角色级覆盖值，两层的层次关系要在脚本
-- 顺序上读得出来，否则后续排查一个取值来自哪一层就得跨文件回溯。
