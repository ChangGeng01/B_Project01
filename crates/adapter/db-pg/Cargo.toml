[package]
name = "ep-adapter-db-pg"
edition.workspace = true
version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
ep-foundation.workspace = true
# 阶段 2 任务 #13：组织架构读取契约 trait 定义在 platform 层，
# 基线第 1.3 节允许 adapter 依赖 platform 端口 trait。
ep-platform-tenancy.workspace = true
# 阶段 2 任务 #14：降级台账端口 DegradationLedger 在 ep-platform-obs，
# 同款允许项；SQL 实现与 gauge 双出落在 platform_ops 目录。
ep-platform-obs.workspace = true
# 阶段 3a 任务 #18：幂等键三态判定纯逻辑在 ep-platform-outbox，
# 同款允许项；SQL 实现体落在 platform_msg 目录。
ep-platform-outbox.workspace = true
# 阶段 4 任务 #21：身份域端口 trait 在 ep-platform-identity，
# 授权集合读取面对接 ep-platform-authz 的六张用户维度表；
# 同款允许项；SQL 实现体落 platform_core/identity*.rs 与
# platform_authz/user_grants.rs。
ep-platform-identity.workspace = true
ep-platform-authz.workspace = true
async-trait.workspace = true
uuid.workspace = true
chrono.workspace = true
# 阶段 4 任务 #23：授权配置写入/快照载入对策略条件 jsonb 的序列化面。
serde_json.workspace = true
# 阶段 2 任务 #11：sqlx 承接五具名池与会话语义；tokio 供退避 sleep 与测试用运行时。
sqlx.workspace = true
tokio.workspace = true
