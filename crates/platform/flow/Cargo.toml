[package]
name = "ep-platform-flow"
edition.workspace = true
version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
ep-foundation.workspace = true
# 守卫表达式要比较金额。基线第 3.5 节把账面金额的 Rust 类型定为
# `foundation::Money`，内含 `rust_decimal::Decimal`——本 crate 与它同源，
# 不另起一套定点表示。
rust_decimal.workspace = true
# `date_add_days` 要做真正的日历加减，不能按字符串拼。
chrono.workspace = true
