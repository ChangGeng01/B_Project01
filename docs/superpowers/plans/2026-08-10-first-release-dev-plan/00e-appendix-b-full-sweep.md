# PRD 附录乙 167 条穷尽核查（2026-08-20）

> 由十二组（U-A 至 U-L）并行穷尽扫描产出，**逐条判定、不抽样**。
> 与 `00d-pending-decisions.md` 的关系：00d 是**候选池抽样两轮核实**的产物（106 条候选）；
> 本表是**附录乙全集**的产物（167 条）。两者交集见第 3 节。

# PRD 附录乙 167 条穷尽核查报告

## 1. 总账：167 条按五档分布

十二组分组数之和 = 15+18+12+18+16+14+7+18+12+16+9+12 = **167**，与 PRD 附录乙实测行数一致（`grep -c "^| U-[A-L]-[0-9]" prd.md` = 167），无缺口、无重复计数。

| 档位 | 条数 | 占比 | 含义 |
|---|---|---|---|
| **已被裁定**（附录乙状态列失真） | **9** | 5.4% | 已由 00c 或 F-10 详本裁死，状态列却仍写「待决」 |
| **规格或 PRD 正文已答** | **21** | 12.6% | 上位文件已有唯一解，本就不需要任何人再拍板 |
| **阶段计划已给取值且低后果** | **90** | 53.9% | 计划层已取值，切换代价为改配置／改一处校验，可不管 |
| **仍需使用方拍板** | **46** | 27.5% | 真正需要人表态 |
| **查不出结论** | **1** | 0.6% | U-C-06 仓库档案归属 |

**这张表的用处**：附录乙状态列现值为 165 条「待决」+ 2 条「已关闭」（U-A-13／U-A-14，核后属实）。也就是说，**读附录乙会看到 165 条未决，实际需要使用方拍板的只有 46 条，虚高 3.6 倍**。其中 30 条（9 已裁 + 21 已答）属于纯粹的回写欠账，一行字都不用讨论。

各组分布（找责任人时用）：

| 组 | 总数 | 已裁 | 已答 | 计划已给 | 待拍板 | 查不出 |
|---|---|---|---|---|---|---|
| U-A 通用 | 15 | 0 | 2 | 9 | 4 | 0 |
| U-B 权限角色 | 18 | 0 | 3 | 7 | **8** | 0 |
| U-C 归属编排 | 12 | 2 | 3 | 2 | 4 | **1** |
| U-D 财务发票 | 18 | **4** | 0 | 11 | 3 | 0 |
| U-E 合同信用 | 16 | 1 | 3 | 10 | 2 | 0 |
| U-F 采购门户 | 14 | 0 | **7** | 4 | 3 | 0 |
| U-G 库存 | 7 | 0 | 1 | 4 | 2 | 0 |
| U-H 总账关账 | 18 | 2 | 0 | **13** | 3 | 0 |
| U-I 报表指标 | 12 | 0 | 1 | 8 | 3 | 0 |
| U-J 售后项目 | 16 | 0 | 1 | 10 | **5** | 0 |
| U-K 低代码客户端 | 9 | 0 | 0 | 6 | 3 | 0 |
| U-L 非功能 | 12 | 0 | 0 | 6 | **6** | 0 |

---

## 2. 已被裁定却仍标「待决」——完整清单（9 条）

这 9 条一条也没回写过。裁定卷的锚点已逐条核过（`sed -n` 直读小节标题，全部存在且内容对得上）。

| 编号 | 主题 | 裁它的是哪条 | 状态列应改成 |
|---|---|---|---|
| **U-C-01** | 交付确认功能由 PRD 第 3 还是第 5 节承载 | **A-09**（00c:413-415） | 已裁定：单据主体归 sales 模块、阶段 6 建表建用例发事件；残留仅 PRD 承载节编排，无技术后果 |
| **U-C-04** | 客户 360 视图无定义节 | **C-09**（00c:1249-1255） | 已裁定：唯一端点 `GET /api/v1/crm/customers/{id}/customer-360`、唯一契约 `Customer360SectionProvider`；阶段 5 建骨架、阶段 12 追加区块；区块清单按规格:286 五类 |
| **U-D-02** | 到款／付款登记后的更正路径 | **F-10 详本 B-3**（:133-144） | 已裁定：资金单据冲正；同批把计划10 F-14 与 00c 庚一第 7 行标为已裁 |
| **U-D-04** | 税率集合＋一票是否多行明细 | **F-10 详本 B-8**（:300-310）**只裁了一半** | 多行明细已裁定（发票带行明细）；**税率可选值集合仍按计划10 F-02 出厂六档，切换代价低** |
| **U-D-08** | 开票内容逐行对应还是自由文本 | **F-10 详本 B-8**（:300-310） | 已裁定：发票带行明细并逻辑引用订单行；PRD 6.4／6.5 字段表同批加行明细 |
| **U-D-09** | 红字发票号码必填／部分红冲 | **F-10 详本 B-4**（:146-153） | 已裁定：号码必填、允许部分红冲、作废仍限全额一次；同批改计划10 F-08 与 00c 庚一第 8 行 |
| **U-E-12** | 合同提前终止与在审撤回 | **F-10 详本 A-5 + A-6a + A-7**（:73／:376／:87） | 已裁定：新增 TERMINATING 态；TERMINATION 审批链且管理层必经；派生物处置七条规则 |
| **U-H-07** | 更正凭证入口 | **F-10 详本 B-1**（:108-121） | 已裁定：开入口，限定为必须引用一张已过账凭证的重分类更正，财务过账高风险类，须重新认证加审批，落阶段 9b |
| **U-H-08** | 手工凭证录入入口 | **F-10 详本 B-2**（:123-126） | 已裁定：不提供；并回写规格第 5.2 章明写「凭证只由十类事件映射、年结、更正凭证三个来源产生」 |

**反向陷阱一条，必须一并知会**：**U-A-08（默认审批链）的状态列不能改成「已裁定」**。F-10 详本 C-3 的「动到」段逐字写了「PRD 附录乙 U-A-08 改已裁定」，但 C-3 的判据覆盖面只有三处（计划04 四条 T0 链、计划06 七值 chain_kind、C-3 点名的六个财务类 scenario）。**主数据四类档案、采购订单、采购与销售退货、信用超额、期初余额、配置发布单、报表发布这七类一条都没覆盖。** 00d 已就此更正（00d:246）。若照 C-3 的动到段回写，会把一条真未决抹成已决。

另：F-10 详本 B-1／B-2／B-3／B-4／B-8 五条明令的回写至今全未执行（00d:534 已实测登记过一次），本轮复核仍然如此。

---

## 3. 「仍需使用方拍板」中未进 00d 那 25 条的新增项

46 条待拍板里，**22 条已在 00d 的 25 条内**（00d 有若干条一条覆盖两个附录乙编号，故不是 1:1）。**新增 20 条**，另有 **4 条属特殊状态**。

### 3.1 新增 20 条（今天没有任何人认领）

| 编号 | 要裁什么 |
|---|---|
| **U-A-07** | 十二类受控取值（客户类型、退货原因、风险类型、工单类型、科目类别…）的出厂清单内容；以及「是否允许管理员增删」取统一口径还是逐类分开——现状是 05 判允许、09 判科目类别随版本冻结、07 与 12 明写不定义取值内容，三处互不一致，改判为固定枚举须停机窗口 |
| **U-A-12** | 开户银行是否与银行账号同列进敏感字段清单；这两列在列表／详情／导出三场景各取什么脱敏形态；导出是否触发重新认证。（银行账号纳入与字段级加密已被规格强制，不在待裁范围。A-28 逐字「本表不代拍」） |
| **U-C-09** | 「已退货未冲回成本」由谁在哪个动作上置位、是否可撤销、撤销后成本归集如何回滚。**这是规格 17.2 的发布前必测项**，今天端口有实现无调用方，标注永不置上 |
| **U-F-10** | 门户五项能力返回字段白名单与逐字段脱敏形态是否获安全负责人批准。**阶段 7 自认这是它唯一的阻塞项，未获批准不得进发布门禁** |
| **U-F-11** | 受限自助注册首版是否开放；开放则须给准入条件、待审核账号的审核人、防滥用取值。现状：代码完整实现但默认关闭，规格承诺两条开通路径、交付只到一条且默认沉默 |
| **U-G-07** | 设备序列号是否复用库存的「法人内唯一」约束，还是改按「同一产品下唯一」。决定同一台设备会不会在库存与设备台账各存一份、客户 360 与库存追溯能否对上。切换含存量去重 |
| **U-H-17** | 补记（记账日期早于登记日）要不要额外权限或额外审批。默认值与可编辑性规格已定死，只剩这一刀——**今天任何有该单据提交权的人都能改期间归属** |
| **U-I-06** | 报表与驾驶舱取数允不允许缓存（物化已被基线定死为否）；允许则同时定缓存有效期与「数据截至时点」展示粒度。翻转需新增失效通道与失效事件，**属结构变更**。这条是 00d 的漏登：同批三条结构性未决中另两条都有着落，只有它两处都没落 |
| **U-J-03** | 设备序列号唯一性范围（与 U-G-07 同源，须同批裁） |
| **U-J-04** | 是否向客户开放以低代码扩展工单状态集合。改判须把状态从 CHECK 改为字典表并占一次停机窗口 |
| **U-J-07** | 已完成／已取消工单是否允许重开及条件与次数上限。现取不允许，返修只能新建工单、历史关联断裂；开通须同步改工单统计口径 |
| **U-J-08** | 换货登记行是否强制退货侧与发货侧配对、是否允许只退不换。**并须同批消解 PRD:3717（需要同时引用）与阶段计划（不强制配对）的正面冲突** |
| **U-J-13** | 合同变更／续签使已派生任务不再需要时取保留／自动作废／新增补充任务。等价于决定影响面机制是否从「合同终止」一个上游事件扩挂到三个。F-10 详本自陈「这一刀比它看起来的贵，需要单独一轮」 |
| **U-K-01** | 低代码配置发布粒度取整包还是按单对象（并发编辑已由内容项级编辑锁解决）。改单对象要拆 `config_release_orders` 与互斥锁，动串行化设计 |
| **U-K-07** | 出厂未做白标配置时的默认产品名称、默认品牌标识、默认客户端包标识（涉商标与商店主体归属，全仓零命中）；并确认 `brand_profiles` 现列集是否为白标可配置面的最终冻结清单 |
| **U-K-09** | 首版交不交付上手引导／示例数据集／可一键重置的演示数据；交付则要定它能否在客户生产环境内安全生成与清除。全仓零命中，`ep-datagen` 是基准数据生成器，不是这个东西 |
| **U-L-05** | 首版是否交付「按客户／模块／文件类型」的多维单文件上限配置对象（含可调区间与调整审计），还是就认一个全局键加门户 50 MB 这一处写死例外。规格逐字承诺「可调」，计划侧无任何配置入口 |
| **U-L-07** | 恢复连接后离线草稿是自动重提交还是必须逐单确认（**直接决定重复提交风险由谁承担**），连带定弱网阈值、离线横幅、自动保存间隔。全卷零承接 |
| **U-L-08** | 不受支持的浏览器／操作系统版本上是硬拦截、警告放行还是完全放行；据此给出具体浏览器名单与最低版本号。PRD:4277 自陈待决，全卷无 UA 门禁登记 |
| **U-L-12** | 搜索结果与派生存储读数要不要在界面标明「可能非实时」及其形态。这是把「被误读为实时数据」的风险留给使用者还是由产品承担 |

### 3.2 特殊状态 4 条

| 编号 | 状态 |
|---|---|
| **U-B-03**（管理者必经审批节点取 ROLE／POSITION／DEPT_MANAGER，是否按金额分档） | 挂在 00d 第 4 条名下但**独立性未验证**（00d:196 自陈）。另有硬伤：**PRD:2295 要求按金额配置审批链，而 `approval_chain_nodes` 与 `clm.contract_types` 两处都无金额承载列——这不是取值未定，是 PRD 要求的一项配置能力在卷内没有落点** |
| **U-B-04**（技术角色能否发起交付确认、能否读合同金额与价格） | 同上，挂 00d 第 4 条、独立性未验证。交付确认是首版唯一收入确认时点，今天没有任何角色能按这个按钮 |
| **U-H-18**（数据责任人能否与财务会计／财务主管兼任） | 同上，挂 00d 第 4 条、独立性未验证 |
| **U-L-01**（并发到顶时排队返 503 还是不限制只记录） | **已提交使用方（00c:6455 逐字「已提交使用方」），至今未见回执**（00d:226）。技术侧现取「等待超 10 秒返 503」＝拒绝登录，与 PRD:4227「平台不拒绝登录」相反 |

---

## 4. 「查不出结论」的条目

### 条目级：1 条

**U-C-06 仓库档案的维护入口归属**。卡点是三方互指成环，不是没查：

- PRD 自己成环：PRD:517「仓库档案见 PRD 库存与仓储节」← → PRD:1850「仓库档案本身作为地点主数据的一个子类型维护……见 PRD 主数据一节」；术语表 PRD:141 把结论悬在本条上。
- 两份计划取值相反：05-master-data.md:14／:922「归 inventory，本阶段不建表」 vs 08-inventory-costing.md:822「仓库档案属性由 mdm 承载，本阶段只读」。
- **结果是全卷没有任何阶段建仓库档案表**：08 第 3.1 节九张表里没有它，14 份计划 grep `warehouses` 零命中；而 08:457 的台账查询要返回 `warehouse_code`、08:823 又要求仓库停用前置校验。
- 00c 与 F-10 详本对「仓库档案」逐字零命中。

**要查清需要什么**：不需要使用方拍板（AI 可以定归属），需要的是**补一次技术裁定**指定建表阶段与建档／停用用例归属。我没有替它取值，因为把它塞进「低后果」档会掩盖一处真实的交付缺口。同源的「地点主数据首版一条不交付」（05:15）与规格:662「MDM 治理范围含地点」也有出入，属同一刀。

### 子问级：11 处（整条已判档，但内部有子问确实无出处）

| 所属条 | 查不出的子问 | 卡在哪 |
|---|---|---|
| U-A-05 | 列表默认列 | 00b:760 指「由各模块在 PRD 中给出」，PRD:594 指回 openIssues，两处互指，全仓「默认列」只这三处命中 |
| U-A-15 | 附件**条数**上限 | 12:895 转指「由平台附件能力统一判定」，平台侧不存在任何条数上限定义（只有 07:832 的 50 MB 字节上限） |
| U-B-04 | 技术角色价格可见性 | 只核到 `field_permissions` 默认拒绝与 06 的 `cpq.price_authorities`，未逐行核阶段 5／6 是否以别的编号顺带给出 |
| U-F-05 | 采购退货审批链**层级** | 全卷无取值；且 07:1144 假设 A15「审批链为空时提交直接进下一态」，即 `approval_required=true` 而链未配置时实际不产生审批 |
| U-F-09 | 风险类型字典**取值** | 归 U-A-07，至今未裁，本组内定不了 |
| U-H-01 | 科目类别↔借贷方向的映射表 | 错误码 `CATEGORY_DIRECTION_MISMATCH` 只在 09:503 出现一次，判它成立的映射表全卷零行；`category` 与 `balance_direction` 是两个互不约束的列 |
| U-H-03 | 科目停用后在历史账表中的可见行为 | 09 与 PRD 7.4 均无规定；三张账表端点不按 `is_active` 过滤故事实上保留，但这是从端点推的、无逐字 |
| U-H-11 | 账表跨期间区间查询 + 试算不平的下钻入口 | 09:940 承诺支持，但 :547／:548／:550 三处端点逐字「必填 accounting_period_id」，只有明细账允许区间；下钻在端点表上无行 |
| U-H-18 | 数据责任人与财务会计／财务主管是否互斥 | 五类管理员互斥集不含业务角色，卷内无一行说它们能不能兼任 |
| U-L-02／03／06 | 进度粒度、前端请求超时、是否可申诉、单用户草稿数量上限 | 四项在 specs+plans 下全部零命中 |
| U-L-01 | 管理员能否查看当前并发人数及刷新频率 | 只查到两个队列指标暴露到运维中心，未查到「当前并发人数」读数的任何登记 |

---

## 5. 诚实标注：证据链弱的组与未查全的面

**必须打折看的三组**：

1. **U-E（合同信用）——最弱**。判「PRD 正文已答」的三条全部是**推导而非逐字**：U-E-01 靠「额度是客户档案字段」+「主数据不跨法人分发」两步推出「按客户加法人」；U-E-04 靠「可用额度＝额度−已占用」+「已占用由三部分闭合枚举」推出「预收不抵减」；U-E-10 依赖对规格:1328 那句自相矛盾表述的读法。**且 PRD:1248 那句「见开放问题」字面上与我的判定相反**——我把它当未回写的债。若使用方不认可这一步，这三条应退回「仍需使用方拍板」。另未通读规格 5.2 章 CLM／OMS 条目全文与 PRD 第 3 节全部小节，检索是关键词定位的。

2. **U-C（归属编排）**。00c 是 7700+ 行，我**未逐行通读，是按主题词 grep**（交付确认／客户 360／计量单位／仓库档案／换货／收款计划／未冲回等）。理论上存在措辞不同的承接未被命中。另 00d:471 把 U-C-01/U-C-02 一并列为「已被裁定」，与 00c:415 逐字「本条不代拍的是 U-C-02」矛盾——我以 00c 为准判 U-C-02 未裁，这处矛盾建议一并回写。

3. **U-B（权限角色）**。判「规格/PRD 正文已答」的三条依据的是**命中行而非通读**（未逐字通读规格 22 章与 PRD 全文）。其中 U-B-08 的结论**会直接推翻一份已成稿的阶段计划取值**（PRD:3916「任一维度不通过即拒绝」＝交集 vs 04:850「取并集」），须有人确认是改计划而不是改 PRD。

**方法面的两条系统性限制**：

- **未通读任何一份阶段计划的全文**（U-F、U-H、U-J 三组均如此声明）。若某条取值以完全不同措辞出现在别的计划里且未提编号，本次不会命中。已实测到一例同型风险：U-F-06 在 11:48 用「本阶段不做假定」表述同一件事而未与 07:1146 对齐。
- **00d 的候选池本身不穷尽**（00d:202 自陈「PRD 附录乙的 U-A／U-E／U-J／U-K 四组未逐条读完」）。因此「本条不在 00d」**不能**当作「本条不需拍板」的证据——第 3 节那 20 条新增项正是这一点的直接后果。
- **全程只读文档，未验证任何代码实现，未修改任何文件。**

**附：卷内正面冲突清单（不是待裁项，是要回改文件的欠账）**

| 冲突 | 权威侧 | 与之相反的一侧 |
|---|---|---|
| 权限维度叠加：交集 vs 并集 | PRD:3916 交集 | 04-identity-authz.md:850 并集 |
| 记录级可见性 | PRD:3922 列全四种来源 | 00c:190 冻结「只显式共享」；05:919 假定按责任人 |
| 供应商资质／价格记录归属 | PRD 表 3（:190、:191）归第 4 节 | 05:923、07:1122 收口到 mdm |
| 供应商状态集合 | PRD:1675／1676 五态含暂停终止 | 05:927「不另设暂停与终止」 |
| 质量记录是否自动生成 | PRD:1651「由采购退货登记自动生成」 | 05:928「只支持手工登记」 |
| 直接费用类归集字段必填其一 | PRD:1436／1479 阻断提交 | 11:48「本阶段不做假定」 |
| 换货是否强制配对 | PRD:3717「需要同时引用」 | 12:882「不强制配对」 |
| 敏感导出判据 | 04:859 三条件任一 | 11:44「一律按敏感导出处理」 |
| 账龄分档档数 | 计划10:65「六档」 | 计划11:354-360 实为七行，且 D360_PLUS 的 `from_days=361` 与标签「逾期 360 天以上」不符 |
| 并发到顶行为 | PRD:4227「平台不拒绝登录」 | 00b:765／04:860 等待 10 秒返 503 |