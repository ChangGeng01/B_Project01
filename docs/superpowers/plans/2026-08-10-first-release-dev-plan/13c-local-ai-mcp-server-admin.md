> **F-57 状态：`SUPERSEDED_DO_NOT_EXECUTE`。** 首版本地模型、固定九进程、第五客户端和旧 MCP 闭集已被取代；只允许从中提取未冲突的隔离测试。

# F-55 Local AI, MCP, ServerAdmin, and Deployment Carrier Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 F-55 冻结的本地分析 AI、双向 MCP、ServerAdmin 第五客户端以及客户自控物理机/境内 IaaS VM 两种承载体，在一个不可拆分的首版发布批次中集成，并把六项 F-55 门禁、F-56 共同许可证/模块生命周期门禁、禁用态证明与候选证据完整交给 Stage 14b 作最终认证。

**Architecture:** 三条实施线分别拥有 AI、MCP、ServerAdmin/承载体的代码和测试，依靠冻结的 Rust contract、九条全局迁移、`ClientKind` 与发布门禁汇合。Stage 14a0 generic evidence/migrations 可在阶段 1、2 后独立完成；Stage 14a1-F56-adapter 只在 F-56 Stage 3b-2 types/runtime/table 后接入 signed-grant query 与 license/module gate adapter，并且必须先于本计划 entitlement wiring。F-56 Stage 13b 的签名配置包/ServerAdmin 组合仍须在依赖其终态的集成验收和 Stage 14b 前完成。Stage 14a0 冻结并登记其自有 28 个 `V20261023...` 迁移（末项 `V20261023092600`），Stage 6 另有同日期的 `V20261023092700`、`V20261023092800` 两条，故这组全局 pre-F55 `V20261023...` 尾段 exact-set 恰为 30 条且末项是 `V20261023092800`，不得把后两条误归 Stage 14a0。13c 执行首条 F-55 迁移前还必须由 `PreF55DatabaseAdmissionV1` 在 fresh PostgreSQL 16 上从零执行所有 `<20261024090000` 的全局迁移，并证明 F-56 对既定 Stage 3/13 迁移的终态扩充已经生效。随后 F-55 才按 `20261024090000` 至 `20261024090800` 全序列集成，避免向共享数据库后加入低版本迁移。Stage 13c 不产生最终发布结论，真实认证和证据包只由 Stage 14b 完成。终态仍是单台 Windows Server 2022 原生部署、PostgreSQL 16、现有员工 HTTPS 与命名管道拓扑。

**Tech Stack:** Rust 2021 workspace、`candle-core=0.11.0`、`candle-transformers=0.11.0`、`tokenizers=0.23.1`、QWEN2/GGUF v3/MOSTLY_Q4_0、Axum、PostgreSQL 16、SQL migrations、Windows Service Control Manager、Windows named pipes/Job Objects/Credential Manager/HCS、React 静态 SPA、OpenAPI 3.1、真实 PostgreSQL 集成测试、Windows Server 2022 实机门禁。

**Specs:** `docs/superpowers/specs/2026-08-22-f55-approved-ai-mcp-server-admin-cloud-freeze.md`；许可证、签名模块包、AI/MCP entitlement 与共同门禁以 `docs/superpowers/specs/2026-08-22-f56-license-signed-module-package-freeze.md` 为唯一后续权威。

## Global Constraints

- 本计划只在用户另行授权后执行；当前文档冻结不运行产品开发、迁移、构建、测试或发布。
- Stage 14 相关依赖固定为 `Stage 1/2 → Stage 14a0`，以及 `(Stage 14a0 + F-56 Stage 3b-2) → Stage 14a1-F56-adapter → Stage 13c entitlement wiring → Stage 14b final certification`；14a0 与 Stage 3b-2 可并行。Stage 13c 依赖 F-56 Stage 13b 终态的其余集成点仍按本计划 DAG 等待，不得把 Stage 13b 错升为 14a0/14a1 的开工前置。F-56 前置必须按其实施计划完成许可/模块表、20-item config package 终态、两个 special applier、ServerAdmin multipart/只读快照和 `ModuleLicenseQuery`；不得在 13c 重做或用临时 DTO 越过。Stage 14a0/14a1/14b 都仍由阶段 14 拥有：14a0 交付 `platform_ops.deployment_records`、当前版本选择器、服务器/备份证据 contract 与 fixture、统一 bounded strict-JCS/typed opaque ref/摘要/签名校验原语、十五值 `Stage14GateCodeV1`、通用 gate index/result ABI、fail-closed 的 `ep-release-gate` registry/CLI，并把阶段 14 第 3.4 节全部 28 个自有 `V20261023...` 迁移（末项 `V20261023092600`）写入 catalog；14a1 只交付 F-56 query/adapter；13c 只能消费和扩展，不得复制所有权；14b 才能运行真实探针、签署 index/result、把 gate 判为通过或组装发布包。
- 任一可变共享数据库必须先通过 Stage 14 第 0.0 节的 `PreF55DatabaseAdmissionV1`：按全局版本补齐并验证所有 `<20261024090000` 的迁移，Stage 14 自有 28 文件虽止于 `V20261023092600`，同日期的全局 pre-F55 尾段还须接续 Stage 6 的 `V20261023092700`、`V20261023092800`，exact-set 合计 30 条并以后者为末项；随后才执行本计划九个 `V20261024...` 迁移。Stage 14b 不得补写、重编号或执行此前未登记的低版本迁移；14a0、14a1 或 13c 完成均不构成部分发布、认证通过或上线许可。
- 三份 F-55 子计划是唯一实现分解：[本地 AI](../2026-08-22-local-ai-implementation.md)、[双向 MCP](../2026-08-22-mcp-extension-implementation.md)、[ServerAdmin 与承载体](../2026-08-22-server-admin-cloud-carrier-implementation.md)；它们共同消费且不得复制 [F-56 许可证与签名模块包实施计划](../2026-08-22-license-module-package-implementation.md)。
- 现有 69 个已执行迁移的文件名、内容与 checksum 一律不改；F-55 九个版本先在 Task 1 一次性保留，实际 SQL 由三条实施线按 owner 分批提交；九个文件全部存在并通过 Task 5 exact-set/fresh-database 校验之前，任何一个都不得对共享开发库执行。
- F-55 迁移严格按 `V20261024090000`、`090100`、`090200`、`090300`、`090400`、`090500`、`090600`、`090700`、`090800` 顺序；生产逻辑回退只停路由或停用记录，禁止删除表、列或历史数据。
- 终态恰好九个产品常驻进程；只新增 `apps/ai-inferer`，MCP 与 ServerAdmin 不新增产品进程或监听端口。
- `ClientKind` 恰好八值 `win|mac|ios|android|portal|ops|server_admin|mcp`；audit client 恰好再加 `system` 成九值；MCP 不进入客户端能力矩阵。
- AI/MCP entitlement 的唯一来源是 F-56 `ep-platform-license::ModuleLicenseQuery` 对同一 current/history signed grant 的逐次重建与验签：AI 只查 `EntitlementCodeV1::F55LocalAi`，入站和出站 MCP 共查 `EntitlementCodeV1::F55Mcp`；`Active|ExpiringSoon|GracePeriod` 才 currently licensed。`purchased` 只认 current/history 中在现行 bundle 下仍为 `TRUSTED`、未被标记 `HISTORICAL_SIGNER_REVOKED` 的相应 entitlement，不放行请求；正确分类的 `HISTORICAL_SIGNER_REVOKED` 是保留/隔离历史的 accepted-containment 包含态，不计 purchased/rollback/正向证明，但本身不是 common-gate FAIL：若其首次接受 bundle exact bytes、唯一接受审计/hash-chain、source/payload/two-CMS 与 CRL containment 全自洽，且独立 current 为 `TRUSTED`（两层 ACTIVE，或非撤销 RETIRED 并有同 origin/chain 的首次 ACTIVE exact 证据），其他共同 predicate 全过时 gate 可 PASS；只有误分类、证据缺失或非 CRL 漂移才阻断。禁止 F-55 私有许可 payload、旧四态、配置布尔值、模块码、feature flag、环境变量或人工 JSON 冒充许可。AI/MCP 是平台 capability，本身不得映射或伪造第十六个 `ModuleCode`；平台入口先判 entitlement，只有请求读取或操作具体业务对象时，才在读取该对象 payload、推理使用其字段、动态写/批或 egress 前逐次通过该对象在编译期 registry 中已有的 owner-module effective-runtime gate。entitlement 缺失/失信或真实 owner module 不可用均失败关闭，两条判定互不替代。全局 `Restricted` 本身按 `LicenseAdmissionEffectV1` 而非按端点名称裁切：AI 纯读取/草稿与 `ReadReportAuditBackupExport` 继续可用；有副作用的 MCP 出站、动态 `Write|Approve`、普通业务写入/审批或新自动化被拒绝时，唯一稳定平台码是 `PLATFORM.LICENSE.RESTRICTED=BUSINESS_CONFLICT/409/false`。AI/MCP 各自的领域错误闭集不扩张，但经过通用 admission middleware 的端点必须允许该平台错误作为外层拒绝结果，不能把“route-specific domain union”误写成永不返回平台码。
- `Restricted`/无 current 的恢复例外闭集只允许 `LICENSE_GRANT` 的 import→autotest→submit→Win/Mac `CONFIG_RELEASE` 审批结论→sign→release-order/execute，以及 `MODULE_PACKAGE:DISABLE` 的同链；原权限、双人职责分离、内外签名、部署绑定和审计全部保留。普通配置项、模块 `INSTALL|ENABLE|UPGRADE|ROLLBACK_VERSION`、其他业务审批、AI/MCP 调用或 ServerAdmin approve/reject 都不能借用该例外。
- F-56 的签名模块包只是统一产品内置模块的声明式 `LICENSE_GRANT|MODULE_PACKAGE` 单项 imported config package；不得携执行体。模块 install/enable/disable/upgrade/rollback-version、许可四态、永久/订阅边界、双重签名、`ModuleOperationGate`、30 秒排空超时和数据保留全部由 F-56 计划唯一拥有；本计划只验证其结果对 AI/MCP applicability、ServerAdmin 只读视图和共同 `RG-LICENSE-MODULE-LIFECYCLE-GREEN` 的接入。许可 current 与 15 行 module current 是独立信任域：current module 失信只关闭该 module effective runtime，不改 deployment `LicenseStatus`；ServerAdmin 每行必须显示由现行 bundle 重算的 `package_trust_status=NOT_INSTALLED|TRUSTED|SIGNER_REVOKED|INVALID`，不得以 install/enable state 冒充信任态。
- 能力矩阵恰好 `18 × 5 = 90` 行；ServerAdmin 只有 `platform.admin_lowcode_ops`、`platform.extension_dynamic_code` 为 `FULL`，`platform.approval_notify`、`platform.full_text_search`、`reporting.report_print` 为 `VIEW_ONLY`，其余十三项为 `NOT_APPLICABLE`。
- AI 只生成单数据集 `AiQueryPlanV1`；查询结果和结果派生文本进入 `ai-inferer` 的字节数必须为零。
- Runtime ABI v1 固定 `candle-core=0.11.0`、`candle-transformers=0.11.0` 与直接依赖 `tokenizers=0.23.1`；只有直接 tokenizers 声明 `default-features=false`，non-wasm 解析闭包必须含 Candle 固定带入的 `tokenizers/onig` 与 `onig/onig_sys`，但禁 `http`。模型固定 QWEN2 + 单文件 `model.gguf` + GGUF v3 + `general.file_type=MOSTLY_Q4_0` + 独立 `tokenizer.json`；已知 rank-2 matrix tensor 必须 Q4_0、rank-1 norm/bias 必须 F32，tensor 缺失、多余或 dtype 错均拒绝。CPU_LOCAL 为基线，GPU_LOCAL 只来自同版 Candle `cuda` feature 的另行 Authenticode 签名/SBOM/Stage 14b 认证构建。
- 解码固定 `GREEDY_SCHEMA_DFA_V1`、temperature=0、`max_new_tokens=2048`、schema UTF-8 字节前缀 DFA、同 logit 取较小 token id；完整单个 JSON object 后只接受 token id `151645` 且其解码必须为 `<|im_end|>`。禁止 sampling/seed/beam/top-k/top-p、hub 下载、Python、ONNX Runtime、llama.cpp/ggml FFI、自定义 op、脚本解释器、运行期动态库或依赖下载。
- Candle/tokenizers 版本、feature 集、Cargo.lock、离线 vendor 摘要和 CycloneDX SBOM 必须同一发布批次冻结；AI compose locale 首版只接受字面量 `zh-CN`。
- AI compose 是普通同步 HTTP 8 秒/20-slot 闸门的唯一 AI 例外：基础 body/header/session/法人校验后进入固定公平 45-slot gate（15 running + 30 queued），无位不建目录/pipe；内部 120000 ms 从入 gate 起覆盖排队/IPC/推理，外层 122000 ms 的余量只作取消/清零/封套，超时统一 `PLATFORM.SYSTEM.SYNC_TIMEOUT`。断连立即删除未开始的排队项；已运行项协作取消并清零本请求 decoder/KV。首版无 per-invocation 子进程且不强杀 Rust 线程；2,000 ms 未确认终止即关闭 readiness 并终止整个 `ai-inferer`/`APP_AI` Job，使全部同进程在途请求取消、清零并以 `AI.MODEL_PACKAGE.NOT_ACTIVE` 失败。重启后只有独立包复验、fresh activate ACK 与当前认证 gate 全过才重开；不继续算到 120 秒、不交付受影响结果、不复用旧 ACK。
- AI compose 与 execute 共同构成且只构成已认证只读 action POST 幂等豁免闭集：两者都绕过写请求幂等中间件、零读写 `platform_msg.idempotency_keys`，携带 `Idempotency-Key` 均以 `AI.QUERY_PLAN.IDEMPOTENCY_NOT_ALLOWED` 拒绝；不得类推第三个豁免端点。
- AI 模型包同时唯一约束 `(model_code,model_version)`、`package_digest` 与 `install_receipt_id`；ops 安装只经 exact `ops.signed_artifact.install_receipt.v1` + `SignedArtifactInstallReceiptV1`。core 只读 receipt/config/digest 与安装根 owner/DACL，在同一事务插入 REGISTERED 并走合法边到 VERIFIED，`verified_at` 固定为首次服务端 receipt 事务时点且此后不可改；REGISTERED/VERIFIED/CERTIFIED/ACTIVE/DISABLED/REVOKED 的五时间、证据 ref/digest 和重认证轮次严格服从 F-55 truth table。正文独立复验仍由 Stage 14b 与 ai-inferer 完成。compose 只有在 DB ACTIVE 与 `ai.model.activate.v1` 独立复验 ACK 完全一致时开放，deactivate/重启/pipe 断开均先 fail-closed；weights 只读共享，每请求 KV 独立并销毁。
- shared receipt `manifest_digest` 的语义按 artifact kind 固定：AI=inner `AiModelPackageManifestV1` exact JCS digest，MCP=数据库已批准 connector `McpManifestV1` canonical JCS digest；inner `McpLocalArtifactManifestV1` 只由 exact CAB package digest 绑定并每次从 CAB 复验，不能写进 receipt/DB 同名字段。receipt UUID 先取 exact transaction advisory lock 再跨 AI/MCP 表查重；lost-ACK replay 排除本次 transport `request_id`、比较 schema/kind/package/manifest/root/installed-at/full-subject 全部持久语义，ACK 恰为当前 `request_id,receipt_id,registered_object_id`，没有 acceptance timestamp。
- AI 模型只允许一个非 spanning CAB，exact bytes 1..2,147,483,647，七 entry 解包总≤2,130,706,432 且 `model.gguf`≤2,000,000,000；低成本 CPU baseline 必须落入此界，更大模型须新 Runtime ABI/包装裁定。安装根 regular-file roster 恰为固定 `package.cab` 加七项 extracted entry；安装器二次读回、ai-inferer 每次激活都核 CAB digest/Authenticode、exact roster、inner CMS 与 archive-entry↔extracted length/hash 后才 mmap 权重。
- AI 资源认证只接受最大 1,048,576-byte strict RFC 8785 `AiResourceCertificationReportV1` 和 strict `AiResourceCertificationSignatureV1` sidecar，后者字段 `signature_p1363_b64url: String` 只接受 RFC 4648 §5 canonical no-pad、解码恰 64-byte P-256 low-S P1363 且重编码逐 byte 相同，拒绝 integer array/hex/DER/padding。purpose `AI_RESOURCE_CERTIFICATION_V1` 与 release/model/build/server/load/gate 绑定必须全部通过。ref 只能是 `ep-evidence://ai-resource/<release-batch>/<model-package>/<cpu-local|gpu-local>/sha256/<digest>`，只经编译期读取根解析；DB/API/审计只保留 ref/digest/结论。
- `AiRuntimeReleaseFactsV1` 只在 Stage 14b 当场从签名 ai-inferer/Cargo.lock/offline vendor/SBOM/resolved features 生成最大 262,144-byte strict JCS；不持久、无独立签名/有效期/DB 行，现场重算值只进入同 run 的最终签名 AI resource report，而该 report 以 mandatory `evidence_code=ai_runtime_release_facts` typed entry 进入 `RG-AI-RESOURCE-CERTIFIED` 的 `Stage14GateEvidenceIndexV1`；不得向 exact `Stage14GateResultV1` 增字段。认证/激活时仍从保留制品重算。AI audit 只在编译期 registry 登记 `reporting.ai.query_turn` 与 exact `AI_QUERY_PLAN_COMPOSED|AI_QUERY_PLAN_EXECUTION_ATTEMPTED`，before NULL、strict masked after；不得另建 registry 或保存问题/计划/SQL/结果/prompt/token/model-output 正文。
- AI plan token 固定法人密钥域 `KmsBackend` purpose `AI_PLAN_TOKEN_V1`、P-256 canonical low-S P1363 raw64、JCS exact claims 和 `epai1.<key-version>.<claims>.<signature>` framing；只有 ACTIVE key 签发，旧未吊销 key 只在最后合法 token 到期后 60 秒窗口内验证。不得用进程内对称 secret 或跨法人 key。
- AI execute 的 8,388,608 bytes 是 compact UTF-8 JSON 成功响应整个 envelope/columns/rows 的上限；有界 sink 在下一完整 JSON token 会超限时丢弃全部结果并返回 `REPORTING.ANALYTIC_QUERY.RESULT_TOO_LARGE`，不返回部分行，不用 `truncated=true` 掩盖字节超限；`truncated` 只表示 `limit+1` 行截断。
- AI reporting 两端点的领域错误闭集逐 route 冻结，不能以注册表并集代替：compose 才能在结构合法计划引用当前裁剪目录中不存在/不可见 dataset/field code 时返回统一 404 `AI.QUERY_PLAN.NOT_VISIBLE_OR_DENIED`；execute 永不返回该码，token 无效用 `AI.QUERY_PLAN.TOKEN_INVALID_OR_EXPIRED`，合法 token 所绑目录/授权当前事实变化用 `AI.QUERY_PLAN.SECURITY_CONTEXT_CHANGED`。两端点都含 `PLATFORM.REQUEST.INVALID_PAYLOAD|PLATFORM.REQUEST.HEADER_MISSING`；execute 另含三个 reporting 查询错误及 `PLATFORM.SYSTEM.SYNC_TIMEOUT`。通用 admission middleware 的平台错误与该 domain union 正交：AI 纯读取/草稿在全局 Restricted 下不被它拒绝；任何后来被分类为有副作用的入口若由通用 gate 拒绝，可在领域 handler 前返回 `PLATFORM.LICENSE.RESTRICTED`，不据此扩张本段领域 enum。`confirmed` 是 required ordinary boolean，false 是合法 shape 但返回 409 confirmation-required，不可在 schema 写 `const:true`。
- AI reporting 两端点的 `X-Client` enum 恰为 `win|mac|ios|android|ops|server_admin`，拒绝 portal/mcp 和旧五值快照；ServerAdmin 仍须 `ReportingReportPrint + Read`、各 route 的 AI permission/object scope、license 与普通安全事实，客户端身份或 `platform.admin.ai_model.view` 均不自动授权 compose/execute。
- MCP 固定 JSON-RPC 2.0、协议 `2026-07-28` 和 `server/discover|tools/list|tools/call|resources/list|resources/templates/list|resources/read` 六应用方法闭集；literal URI 与 RFC 6570 Level 1 template 分列。每个 HTTP 请求用 protocol/method/name headers 自描述且与 body 匹配；入站只即时 JSON，出站客户端另支持同一 POST 内有界 request-scoped SSE。`ping`、initialize flow、应用级 cancelled、Sampling、Roots、Tasks、Logging、prompts、legacy 独立 GET/SSE、DCR、通用 SQL/shell/文件/HTTP 代理全部不可达；`notifications/cancelled` 只允许作 local stdio 传输控制。
- request `params._meta` 是唯一有界 extension-map 例外：三个保留成员必填，另有 0..32 个 namespaced key，按 F-55 exact key/value/depth/container/string/integer/65,536-byte 总界验证后立即忽略；success `_meta` 只含 serverInfo，其他 strict object 不接受扩展。入站 JSON-RPC error data 只有普通 `{stable_code,request_id}` 与 visible-ASCII version-only `{stable_code,request_id,supported:["2026-07-28"],requested}` 两形状，id 总存在/不可判时 null。
- MCP 入站不是 bearer-only：grant token 是完整 50-byte ASCII `epmcp1.` + 恰 32 个随机 bytes 的 43-char RFC 4648 base64url-no-pad；只接受 exact regex/规范 decode→re-encode，数据库与 DPoP 均 hash 完整 50 bytes。每次请求还需来源设备 DER-SPKI ECDSA P-256 canonical low-S P1363 raw64 PoP、Unix 时间 ±60 秒和连续持久 counter，counter 与 used_calls 在同一 per-grant 条件原子更新中前进。manifest 由 `MCP_MANIFEST_V1` release key 签名 `SHA-256("EP-MCP-MANIFEST-V1\0" || connector_id || version_no || SHA-256(JCS(manifest)))`；其 sidecar `signature_p1363_b64url: String` 必须 canonical no-pad、解码 exact low-S raw64 并拒绝 array/hex/DER/padding。credential 是 transport-shaped 的单一 `Option<McpCredentialBindingV1>`。
- grant issue 的 `ttl_seconds,max_calls` 是唯二可选字段：省略分别取当前 `mcp.grant_ttl_seconds`/`mcp.max_calls_per_grant`，显式值必须为 `1..=当前配置`，两配置默认且硬上限为 600/100、只能向下收紧，0/超限统一 invalid-payload。grant 状态边仅 `ACTIVE→CONSUMED|REVOKED|EXPIRED`，后三终态；expiry scanner 只做 ACTIVE→EXPIRED，主动撤销只做 ACTIVE→REVOKED，最后受理 UPDATE 同时计数并写 CONSUMED。grant 的 ACTIVE/remaining 只由该原子受理 UPDATE 判一次；`max_calls=1,counter=1` 可在该语句把行置 CONSUMED，但同一 accepted request 必须继续 current facts/binding/ATTEMPT/dispatch，任何后续 ACTIVE 自检不得反向拒绝它，第二次请求才失败。
- MCP credential 形状唯一：INBOUND 必须 `None`，REMOTE 只能 `None|HTTP_AUTHORIZATION_BEARER`，stdio 只能 `None|LOCAL_SECRET_PIPE_UTF8`，Hyper-V container 首版必须 `None`。remote 只注入 `Authorization: Bearer`；stdio 的 secret 只走专用匿名 pipe，argv 只带非秘密 handle locator；Hyper-V 无 host handle/env 桥接。
- grant issue 禁止 `Idempotency-Key` 且绕开通用幂等 response-body 缓存；精确拒绝是既有 `PLATFORM.REQUEST.INVALID_PAYLOAD` 与唯一 detail `Idempotency-Key is forbidden for this endpoint`。token 明文仅返回一次，丢失响应后只可撤销/等待过期再签发新 grant。`/mcp` 是普通 8 秒/20-slot 闸门的唯一 MCP 例外：基础 transport/body/token identity 后先生成 invocation/event ids 并物理预留、flush caller completion slot，再固定先取 connector-keyed 全局公平 16-slot、再取 per-connector 4-slot；预留失败时零 permit、零 rate、零 counter、零 ATTEMPT、零 dispatch，并发溢出不消费 proof。proof 受理后启动 30000-ms deadline，外层 32000 ms 的余量只作取消/审计/封套。counter UPDATE 成功后的所有响应才带 exact `X-EP-MCP-Proof-Counter-Accepted`，客户端只以该值推进 counter。
- connector 状态边仅 `REGISTERED→PENDING_APPROVAL→DISABLED`、`DISABLED↔ENABLED` 与任一非 REVOKED→REVOKED，REVOKED 终态。DISABLED→ENABLED 在同一 connector-keyed transaction advisory exclusive lock 内重读 row version，并要求唯一 compatible ACTIVE manifest、有效签名/未吊销 key、LOCAL 完整物化或 REMOTE origin/当前 credential probe、license 和对应 MCP gates 全绿；任一失败保持 DISABLED。REVOKED 令所有 grant 失效，但 connector/version/grant/audit 历史全保留。
- MCP rollback 复制旧内容为 `max(version_no)+1` 新行并重新签名/测试/审批，绝不重开 SUPERSEDED。两种 LOCAL 都只允许一个非 spanning CAB，exact bytes 1..2,147,483,647；stdio 解包≤2,000,000,000，Hyper-V `image-layout.tar`≤2,000,000,000 且 OCI expanded≤5,368,709,120。artifact kind wire 只取 `MCP_SIGNED_STDIO_PACKAGE|MCP_WINDOWS_HYPERV_CONTAINER` 并与 transport/outer receipt kind exact 匹配；`artifact_code/version` 只是有界签名显示身份，不参与路由/权限/兼容。stdio extension→MIME 与 OCI index/manifest/config/four-layer MIME 都使用 F-55 闭集，拒绝 Rust alias、Docker/zstd/OCI-1.1/foreign/unknown MIME。每个 root 只含固定 `package.cab + exact archive entries`，安装二次读回与 plugin-host 每调用独立复验 package digest/closed roster/inner CMS/archive-entry↔extracted equality。LOCAL_SIGNED_STDIO 每个 manifest version 使用独立 `<manifest-version-uuid>/<package-digest>` 根，即使 CAB 相同也完整复制，禁止 hardlink/reflink/block clone/共享文件对象；必须同时具备 exact-SID zero-capability AppContainer/LPAC、restricted zero-privilege low-IL token、包 ACL、只继承 stdin/stdout/NUL-stderr/可选 secret-read 的显式 handle list、由 ops-agent 在六个 IPv4/IPv6 connect/receive/bind ALE layer 预建且按冻结 UUIDv5 key 查询的 persistent WFP 阻断和 Job Object。WFP provider/sublayer/filter 使用 F-55 exact persistent flags/weight、SYSTEM owner/Administrators group 和唯一四 ACE 权限模板。可选 `LOCAL_WINDOWS_HYPERV_CONTAINER` 只允许 HCS Hyper-V isolation，且只按 strict OCI config 的单 `.exe` Entrypoint/`C:\EP\Plugin`/`ContainerUser`、签名 app PE/DLL 闭包和签名 Windows base-layer allowlist 启动；禁止 process isolation。宿主/BC-2 nested 证据缺失只禁该 transport。其唯一写层是每调用 536870912-byte HCS scratch，先销毁 compute system 再清理。plugin-host/child/guest 的 WER 上传与 full/user dump 必须禁止并过 crash-marker 零泄漏 canary。
- MCP remote/local 两个 exchange operation 只使用独立 `McpExchangeChunkStreamV1` / `McpExchangeChunkStreamFrameV1` 承载 256 KiB manifest、1 MiB 请求和 8 MiB 响应；frame 恰九项：请求三帧、无 ACK 的 `DispatchAuthorized`、响应三帧、Abort、Ack。`McpExchangeRequestV1`/RequestBegin invocation id 相等，wire 不带 wall-clock deadline；request/response 使用不同且关联的 stream id 和独立 sequence，Ack/End 用 `next_sequence_no`。receiver 在 RequestBegin 用本机单调时钟启动不可重置的 30 秒。RequestEnd COMPLETE 只代表请求已验证、permits/rate 已保留且零 dispatch；caller 随后独立提交 deterministic ATTEMPT，成功才发 `DispatchAuthorized` 允许 receiver 恰 dispatch 一次，失败用 `AUDIT_UNAVAILABLE` Abort 且零 dispatch。Begin/authorization 无 ACK，Chunk/End/Abort 分别只能得到 `CONTINUE|COMPLETE|ABORTED`，七 abort reasons 含 `RATE_LIMIT|AUDIT_UNAVAILABLE`，错态至多一个 Abort 后清零关闭。response chunk 只承载 exact terminal JSON-RPC bytes，不带 reply wrapper；ResponseEnd COMPLETE 只验证 raw length/sequence/SHA-256，caller 此后才 size-first strict parse/schema/field，失败写 terminal completion且不反转 ACK、退款或重放。terminal 上限 8,388,608 bytes，SSE 只有一条 exact event、固定多 23 bytes 且 decoded body 上限 8,388,631。超字节一律 `MCP.PAYLOAD.TOO_LARGE`，大小合法后才可返回 schema-invalid。零落盘/零恢复，绝不复用阶段 3 `BoundedChunkStreamV1` 的 10/30/3600 秒大文件语义。LOCAL 必须携带与当前不可变 manifest 完全匹配的安装收据物化事实，附件不能直接执行。
- LOCAL manifest 的 receipt/materialization tuple 在 DRAFT/PENDING_APPROVAL/APPROVED 可 joint NULL，只允许 APPROVED 时一次性 all-null→complete，ACTIVE 前完整且此后不可变；rollback 新 DRAFT 不复制旧 tuple。
- LOCAL 的完整 materialization group 恰为五个 receipt/root/time/HCS/root-SD 列加十个 sandbox profile/SID/provider/sublayer/六-filter-key 列，共十五列；stdio 要求 HCS 空且十个 sandbox 值全有，Hyper-V container 要求 HCS 有且十值全空。receipt UUID 写入前执行 F-55 exact transaction advisory lock 并交叉查询 AI/MCP 两表，禁止跨表重用和任何 writer 旁路；卸载还要求所有引用版本均为 `REJECTED|SUPERSEDED|REVOKED`、零进程/句柄且不在 newest two。
- gateway/plugin-host 的唯一 manifest 验证根是固定路径下最大 1,048,576-byte strict `McpManifestTrustBundleV1` JCS + detached CMS，同一签名离线 CAB、exact SYSTEM/ep-ops/ep-integ/ep-plugin DACL；ACTIVE/RETIRED-nonrevoked P-256 SPKI entries 才可验 exchange。更新只在 MCP gate 关闭且两 adapter 停止时 staging 验证并 write-through 同时替换两文件后重启，运行中 identity/digest 漂移立即关 gate，禁在线/热/单文件替换。
- MCP inbound、remote（core/worker）与 local（core/worker 的 stdio/Hyper-V container）全部使用同一 attempt/completion/spool/crash-reconcile 契约；cancel control 无独立事件但原 invocation 必有 completion。入站/core 出站由 core-server 拥有 audit identity/DB/spool，job/Outbox 出站由 job-worker 拥有；gateway/plugin-host/child/guest 永不写 DB/spool。core/worker 各有固定 1-GiB/1024×1-MiB 专用 completion spool；identity 后先物理预留 `.reserve`，terminal 用同一 slot 走 `.tmp`→`.ready`→DB，JCS 上限 1,048,507，完整 tmp 升 ready，损坏件改 `.corrupt` 保留取证，绝不重派。审计使用冻结 namespace/object/event-id 和 strict after 字段：pre-decode/四个无具名方法保持 request/binding 可空真值，tools/call/resources/read 仅在 decode 后写 name hash、仅 binding 解析后写相应 binding/schema/input；无 ATTEMPT 只可 REJECTED，有 ATTEMPT 才可成功/失败/超时/取消/崩溃未知，terminal bytes 的 hash/1..8MiB 与 schema-valid output fields 分开记录。只存 binding/manifest identity 与 `decoded_name_sha256` 等摘要事实，不落 raw name/展开 URI/对象 id/header/secret/payload；身份未解析的拒绝只写五字段部署安全日志。`ep-worker` 只可在 ep-integ/ep-plugin 上分别调用 `mcp.remote.exchange.v1|mcp.local.exchange.v1`，不因此取得通用 HTTP/本地执行权。
- MCP rate 是每 connector 无 burst 的单调时钟 60 秒 sliding window/60 次；core、integration-gateway、plugin-host 分别是 inbound、remote（合并 core+worker）、local（合并两 transport）唯一 owner。INBOUND 顺序固定 identity/proof syntax→ids→completion slot→global 16→connector 4→rate→counter UPDATE→facts/binding/authz→ATTEMPT→dispatch；ATTEMPT 失败在 counter 后，返回 accepted header + `MCP.AUDIT.UNAVAILABLE` 且零 dispatch。OUTBOUND 固定 caller identity→ids→slot→专用 DB connection/session shared lock `hashtextextended(lower(connector_id::text),5568499270196608077)`+首次 ENABLED/ACTIVE/identity/authz/binding 重读→receiver permits/rate→RequestEnd COMPLETE→同锁最终重读→caller ATTEMPT→DispatchAuthorized→dispatch→terminal→unlock；zero-DB receiver 不查 current connector，enable/disable/revoke/ACTIVE manifest switch 以同 key transaction exclusive lock 串行。成功 reservation 在 inbound counter 成功或 outbound reservation 成功后永不因 audit/外部/响应失败退还；冷启动/状态丢失先灌入 60 个当前 synthetic timestamp 并冷却 60 秒。`MCP.AUDIT.UNAVAILABLE` 固定 HTTP 503/JSON-RPC -32000/不可重试，覆盖 slot、audit、flush/replay/结果确认不可用。
- 普通 8 秒业务 API 不得同步等待 outbound MCP 的 30 秒 exchange；remote/local exchange 只能源自 `/mcp` 专用 route 上下文或 non-HTTP internal job，其他 owner 集成走 job-worker/既有 Outbox，首版不新增同步 outbound MCP route。job 信封必须携原始非空法人/用户/设备行/session/request identity 并在执行时重验；纯 system/丢失来源零 rate/attempt/dispatch。F-55 strict object/tagged-enum DTO 全部拒绝未知字段，unit enum 只接受冻结 variant/大小写。
- `V20261024090300` 同批幂等 seed F-55 五个固定权限 id `...0310`–`...0314` 与 binding id `...0504`–`...0508`，object type 各等于 code，且不 seed 任何角色授权。
- ServerAdmin AI 管理面是独立 `docs/openapi/ai-admin.v1.yaml` 的两条只读 GET；列表只接受 `cursor,page_size`（默认 50、最大 100），拒绝 `limit/page/sort/filter`，使用 exact `epcur1.<base64url-no-pad(JCS(AiModelPackageCursorV1))>` 与 `{data:{items},meta:{page_size,next_cursor}}`，不得暴露安装根、receipt、证据正文、签名或任何 action。
- 共享 `HighRiskOperation` 注册表恰好七值：原六类业务高风险加运维高风险 `DATA_MIGRATION`。MCP 禁止全部七类，合同终止与审批动作另行禁止；ServerAdmin 只读迁移证据；AI 不得生成或执行数据迁移能力。
- 物理机和境内 IaaS VM 使用同一 Windows Server 2022 安装包、服务、数据库、附件、KMS/HSM、备份、恢复、资源和发布判据；provider 快照不能作为唯一备份。
- Carrier family 全部使用统一≤1,048,576-byte strict JCS/code/ref/UTC/array边界。唯一 validator 接收 `DeploymentRecord + CarrierPolicyV1 + CarrierEvidenceV1 + &dyn CarrierFactProbe`，并只通过 `collect(stage14_run_id,deployment_id)` 取得 exact `CarrierFactProbeResultV1`。它同时核两条发布/部署签名链、UUIDv7 Stage 14≤8h run、digest/不透明 ref、十四列和当前机器/provider/region/SKU/TPM2/vTPM/nested/Hyper-V/客户控制/三维备份事实。policy 固定 vTPM profile `TPM2_QUOTE_SHA256_V1` 与每 provider/region 1..8 个批准 `vtpm_ak_trust_anchor_spki_sha256`；VM 的 exact `VtpmAttestationEvidenceV1` 必带本 run nonce、AK cert chain 与有界 `event_log_b64url`，离线验链至 policy SPKI anchor、验 quote/PCR 并重放 event log。两个 carrier 验 `CustomerControlEvidenceV1`，nested=true 验同 run 的 `NestedVirtualizationEvidenceV1`。`BackupFailureDomainEvidenceV1` 内嵌三个 exact `BackupDimensionProbeEvidenceV1` 并重算 domain-separated digest，其 bundle/ref/投影必须与 CarrierEvidence/current probe 相等；evidence 需 SECURITY 与 OPERATIONS 两名不同 subject/key/duty 的 P-256 授权，完整 JCS 再由 deployment KMS `CARRIER_EVIDENCE_V1` 生成 exact `CarrierEvidenceSignatureV1` sidecar。所有 authorization/sidecar `signature_p1363_b64url` 都是 canonical no-pad String，解码 exact low-S raw64；任何 array/hex/DER/padding、child/run/role/key/preimage/吊销或 opaque-ref/root/DACL 漂移均拒绝。
- OCR、向量检索、知识图谱、RAG、工业协议、外部 AI、SaaS、HA、Kubernetes、托管数据库/KMS、Excel 加载项不得产生制品、路由或隐藏开关；既有 XLSX/CSV 导入导出正例必须保留。

---

## Fixed Deliverables and Ownership

| 实施线 | 权威计划 | 唯一拥有的 F-55 迁移 | 主要可执行门禁 |
|---|---|---|---|
| 本地 AI | `docs/superpowers/plans/2026-08-22-local-ai-implementation.md` | `090000`、`090800` | `RG-AI-CONTAINMENT-GREEN`、`RG-AI-RESOURCE-CERTIFIED` |
| 双向 MCP | `docs/superpowers/plans/2026-08-22-mcp-extension-implementation.md` | `090100`、`090200`、`090300` | `RG-MCP-CONFORMANCE-GREEN`、`RG-MCP-CONTAINMENT-GREEN` |
| ServerAdmin/承载体 | `docs/superpowers/plans/2026-08-22-server-admin-cloud-carrier-implementation.md` | `090400`、`090500`、`090600`、`090700` | `RG-SERVER-ADMIN-MATRIX-90-GREEN`、`RG-DEPLOYMENT-CARRIER-EVIDENCE-GREEN` |

F-56 前置实施线不拥有 F-55 九迁移；它按 `docs/superpowers/plans/2026-08-22-license-module-package-implementation.md` 扩充既定 Stage 3/13 迁移、许可/模块运行时、配置包与 ServerAdmin 组合，并提供共同 `RG-LICENSE-MODULE-LIFECYCLE-GREEN`。共享文件的合并顺序固定为：先完成 Stage 14a0 已冻结的 generic evidence/release-gate contract/全部 `V20261023...` catalog；Stage 3b-2 types/runtime/table 到位后立即完成 Stage 14a1 query/adapter，再允许 13c entitlement wiring；依赖 Stage 13b 的集成点继续等待其配置包终态。随后才合并 `Cargo.toml` 与 foundation 类型、F-55 release item/config/error/metrics 注册表、九个 `V20261024...` 迁移、wiring/route，最后把六项 F-55 gate 接到同一 registry，并把 AI/MCP applicability 共同依赖 F-56 gate 后交给 Stage 14b。任一实施线不得改用其他迁移版本或重复定义许可 payload、`ClientKind`、MCP protocol、AI pipe ABI、carrier enum；13c 不得另建第二套 gate registry、gate result 或 evidence index ABI。

### Task 1: Freeze the shared catalogs, parsers, and migration roster

**Files:**
- Modify: `docs/migration-catalog.md`
- Modify: `crates/foundation/src/error/codes.rs`
- Modify: `crates/platform/runtime/src/config/sections.rs`
- Modify: `apps/core-server/src/config.rs`
- Modify: `crates/platform/obs/src/metrics/registry.rs`
- Modify: `xtask/src/errorcodes.rs`
- Modify: `xtask/src/configdoc.rs`
- Test: `xtask/tests/f55_roster.rs`
- Test: `xtask/tests/errorcodes_catalog.rs`
- Test: `xtask/tests/configdoc_catalog.rs`

**Interfaces:**
- Consumes: F-55 §7–9 的九个迁移文件名、35 个错误码、16 个配置登记族、15 个指标以及三条实施线。
- Produces: `F55_MIGRATION_VERSIONS: [u64; 9]` 测试常量；全目录错误码/配置/指标双向校验；以及在任何子计划开始前已一次性预登记、但尚未被业务代码返回或发射的 F-55 catalog code mirror。

- [ ] **Step 1: Write the failing roster and catalog tests.** 在 `xtask/tests/f55_roster.rs` 定义精确数组：

```rust
const F55_MIGRATION_VERSIONS: [u64; 9] = [
    20261024090000, 20261024090100, 20261024090200,
    20261024090300, 20261024090400, 20261024090500,
    20261024090600, 20261024090700, 20261024090800,
];
```

Task 1 的早期 roster 测试只逐项断言 catalog 中九个**保留项**的 path 字面量、owner、stage、版本、无重复与顺序；此时不得要求实际 SQL 文件已经存在，也不得创建空迁移占位。它还断言九项之前的最大版本小于 `20261024090000`，九项之后新增版本大于 `20261024090800`。实际文件 exact-set、内容非空与 fresh-database 可应用性统一推迟到三条实施线完成后的 Task 5，避免 Task 1 反向依赖后续子计划。同时建立两个 catalog 测试：

- `errorcodes_catalog` 只把第 2、6、7、8、9、10、11、12、13 节中第二至第四列分别为合法 `category/HTTP/retryable` 的登记行当作权威行，不能把文案表或历史说明重复解析；逐节计数必须为 `73/70/36/34/36/50/33/128/35`，pre-F55 小计恰为 460、全局总计恰为 495，且文档↔常量表双向相等。第 2 节新增六项必须逐字等于 F-56，其中 `PLATFORM.MODULE.IN_FLIGHT_DRAIN_TIMEOUT=INFRASTRUCTURE/503/true`、`PLATFORM.LICENSE.RESTRICTED=BUSINESS_CONFLICT/409/false`；除冻结的 `MCP.RATE_LIMITED` 外每个码至少三段，两段例外集合必须恰好为这一项；重复、缺失、额外项和 category/HTTP/retryable 漂移均失败。
- `configdoc_catalog` 只解析 `docs/config-reference.md` 的 `## 2. 登记表` 到下一个 `## 3.` 之前、首列为反引号包裹点分键的登记行，并在去重前拒绝重复；该节必须恰有 328 个唯一配置登记族。含占位符的族必须且只能是 `db.timeout.<池>.statement_ms|lock_ms|idle_in_tx_ms` 三项，`<池>` 必须且只能展开为 `rw|ro|worker|ops`，任何其他占位符、`integ` 或任意段通配均失败。展开后的文档具体键必须恰为 337 个且无重复，并与代码结构产生的 337 个具体点分键做双向 exact-set 相等；用 `integ` 替换 `ro`、缺一个合法池或以同数错误键替换均失败。指标目录恰有 70 个唯一指标，其中 F-55 子集恰为 AI 7、MCP 7、carrier 1，F-56 前置子集恰为 license/module 3；文档↔`REGISTERED` 双向相等且标签闭集一致。

- [ ] **Step 2: Run both tests and capture the expected failures.** Run: `cargo test -p ep-xtask --test f55_roster --test errorcodes_catalog --test configdoc_catalog -- --nocapture`. Expected: FAIL，分别报告迁移 catalog 的九个保留项尚未完整（不是缺少实际 SQL 文件）、`xtask errorcodes` 仍只读 PLATFORM 第 2 节，以及 F-55 代码侧 catalog 尚未一次性登记。
- [ ] **Step 3: Upgrade the global catalog parsers and pre-register all F-55 entries.** `xtask/src/errorcodes.rs` 改为上述九节的全目录严格解析，并保留 code/doc 双向比较。`xtask/src/configdoc.rs` 必须删除当前“任意 `<...>` 匹配任意一段”的 `key_matches` 语义：先只从配置文档第 2 节构造 328-family exact set，验证占位族恰为上述三项，再用固定池数组 `rw|ro|worker|ops` 展开成 337-key exact set，最后与所有配置根结构展开得到的 337-key code set 做两个集合完全相等；同时报告并断言 328/337 和 70 个指标。随后在代码侧同一提交候选一次性加入 F-55 全部 35 个错误常量、16 个配置字段/默认值和 15 个指标描述符。16 个字段先落共享 `AiCfg|McpCfg` 并接入本来就同时消费 AI/MCP 控制面的 `CoreConfig`，从而为全目录双向比较提供真实 code root；后续 ai-inferer/worker/gateway/plugin-host 只复用同一 section 类型并按需接入自己的 root，禁止复制字段或默认值。此步只是遵守“先登记后实现”，不得增加 route/wiring/handler，也不得让任一 connector、AI 推理或 carrier gate 变为可达；三个子计划只消费并核对各自子集，不得重复登记或改写文档 catalog。
- [ ] **Step 4: Complete only the migration catalog reservations.** 把九个迁移的未来 path、owner、stage 与唯一版本一次性登记到 catalog；不得创建空 SQL、第二个迁移号或尚不存在的 workspace member/path。`ep-contract-ai`、`ep-adapter-local-ai`、`ep-contract-mcp`、`ep-platform-mcp`、`apps/ai-inferer` 及 Candle/tokenizers workspace declarations 由 Task 2/4 对应子计划在实际目录同批创建，避免 Task 1 因悬空 workspace path 无法编译自身 catalog tests。
- [ ] **Step 5: Re-run the roster and global catalog gates.** Run: `cargo test -p ep-xtask --test f55_roster --test errorcodes_catalog --test configdoc_catalog -- --nocapture && cargo xtask errorcodes && cargo xtask configdoc`. Expected: PASS，九个迁移保留项各出现一次且不以空文件冒充实现，495 个错误码、328/337 个配置口径和 70 个指标均双向精确相等；F-56 六个 PLATFORM 错误/三个指标已由前置计划实现，F-55 条目已登记但所有尚未实施的能力仍不可达。
- [ ] **Step 6: Commit the integration foundation.** Run: `git add docs/migration-catalog.md crates/foundation/src/error/codes.rs crates/platform/runtime/src/config/sections.rs apps/core-server/src/config.rs crates/platform/obs/src/metrics/registry.rs xtask/src/errorcodes.rs xtask/src/configdoc.rs xtask/tests/f55_roster.rs xtask/tests/errorcodes_catalog.rs xtask/tests/configdoc_catalog.rs && git commit -m "chore(f55): freeze integration catalogs and roster"`.

### Task 2: Build the cross-line contract foundation and freeze disabled-mode red tests

**Files:**
- Reference: `docs/superpowers/plans/2026-08-22-license-module-package-implementation.md`
- Reference: `docs/superpowers/plans/2026-08-22-local-ai-implementation.md`
- Reference: `docs/superpowers/plans/2026-08-22-mcp-extension-implementation.md`
- Reference: `docs/superpowers/plans/2026-08-22-server-admin-cloud-carrier-implementation.md`

**Interfaces:**
- Consumes: Task 1 shared catalog mirror、已完成 F-56 Tasks 1–5 后的 `ModuleLicenseQuery`/special applier/ServerAdmin import-bootstrap contract、既有 `ep-foundation` 与 Stage 14a0 generic evidence foundation；凡本任务触及 entitlement query 的 wiring 还硬前置 Stage 14a1-F56-adapter。
- Produces: shared signed-artifact receipt ABI、AI/MCP strict contracts，以及在任何 route/wiring 前已经观察到失败的 disabled-mode suite；不会创建 F-55 SQL、开放 AI/MCP 路由或执行实际 Stage 14 认证。

- [ ] **Step 1: Freeze the six source documents.** Read F-55 §§2–10、F-56 全文、F-56 实施计划和三份 F-55 child plan together, then run `git diff --exit-code` against these six files at implementation start. Any drift requires document review before code work. Before continuing, run the F-56 plan's Stage 3b/13b focused verification and require its schema、20-item config-package terminal shape、ServerAdmin multipart/bootstrap and runtime contract green; this is an admission check, not a second execution of F-56 Tasks 1–5.
- [ ] **Step 2: Execute only the independent contract tasks.** In this exact order run every checkbox of Local-AI Task 1 and MCP Task 1. Internal failing-test→implementation→verification→commit order remains unchanged. MCP consumes the shared receipt shape from AI Task 1; neither task creates SQL or application routes.
- [ ] **Step 3: Verify the contract foundation.** Run: `cargo test -p ep-contract-ai --test abi && cargo test -p ep-contract-mcp --test abi && cargo test -p ep-contract-mcp --test mcp_exchange_chunk_stream_abi && cargo xtask archcheck`. Expected: PASS with one shared receipt ABI and no AI↔MCP application dependency.
- [ ] **Step 4: Write disabled-mode tests before any feature wiring.** Execute this master plan's Task 6 Steps 1–2 now, exactly once. Expected: `ep-f55-disabled-mode` fails because the routes/process gates do not exist yet. Preserve the red fixtures untouched until Task 6 turns them green; do not rerun their “before implementation” step later.

### Task 3: Complete all nine migrations as one dependency graph

**Files:**
- Reference: Local-AI Task 2
- Reference: MCP Task 2
- Reference: ServerAdmin/carrier Tasks 1、2、5
- Test: `testkit/tests/f55_ai_schema_pg.rs`
- Test: `testkit/tests/f55_mcp_schema_pg.rs`
- Test: `testkit/tests/f55_deployment_carrier_schema_pg.rs`

**Interfaces:**
- Consumes: Task 2 contracts、fresh pre-F55 database 已含 F-56 对既定 Stage 3/13 迁移的 exact terminal changes，and Stage 14a0's base `platform_ops.deployment_records` migration/model/repository contracts.
- Produces: all nine nonempty F-55 SQL files, the AI/MCP cross-table receipt uniqueness writer, five exact permission/scope seeds and two-value carrier persistence. AI Task 2 and MCP Task 2 are one mutually dependent database slice, not two serially complete tasks.

- [ ] **Step 1: Observe the complete joint red state before the first F-55 SQL file exists.** Execute Local-AI Task 2 Steps 1–2, MCP Task 2 Steps 1–2, ServerAdmin/carrier Task 2 Steps 1–3, Task 1 Steps 1–3 and Task 5 Steps 1–3. Expected: all five focused suites fail for their stated absent tables/checks/enum/columns; do not let a missing peer table make the AI↔MCP receipt race appear covered.
- [ ] **Step 2: Implement the graph in its only valid order.** Keep one working-tree candidate and execute: ServerAdmin Task 1 Step 4 (shared Rust `ClientKind`) → Local-AI Task 2 Step 3 (`090000`) → MCP Task 2 Step 3 (`090100`) → MCP Task 2 Step 4 (`090200`) → MCP Task 2 Step 5 (`090300` plus five permission/binding seeds) → ServerAdmin Task 2 Steps 4–5 (`090400` plus matrix runtime) → ServerAdmin Task 1 Step 5 (`090500/090600`) → ServerAdmin Task 5 Steps 4–5 (`090700` plus carrier validator) → Local-AI Task 2 Step 4 (`090800`, last) → MCP Task 2 Step 6 → Local-AI Task 2 Step 5. The final AI repository step is deliberately after the MCP tables so its two-table receipt lock has a real peer. Do not run a child verify/commit or touch a shared database in the middle; no empty placeholder is permitted.
- [ ] **Step 3: Verify the one atomic schema candidate.** First execute the exact six-condition `PreF55DatabaseAdmissionV1` from Stage 14 §0.0 against a disposable fresh PostgreSQL 16 database: it must execute every registered migration `<20261024090000` from zero, prove the Stage 14-owned 28-file subset ends at `V20261023092600`, prove the global pre-F55 chain ends at `V20261023092800`, and pass catalog/checksum/rollback/static-projection assertions. The same admission must prove F-56's existing-migration terminal facts without rerunning its tasks: Stage 3 `ItemKind`=18, Stage 13 terminal `ItemKind`=20, the exact license/module columns/current-slot/self-FK/candidate key and three source composite FKs are present. Only after that admission is green, continue on the same disposable database with Local-AI Task 2 Step 6, MCP Task 2 Step 7, ServerAdmin Task 2 Step 6, Task 1 Step 6 and Task 5 Step 6, then run `cargo xtask sqlcheck`. Expected: all nine F-55 files follow `V20261023092800` in `090000..090800` order; nine catalog paths equal nine actual nonempty files; AI↔MCP same-receipt race has exactly one winner; permission/binding/role-grant counts are `5/5/0`; ClientKind/Rust is 8, persistent device is 7, audit is 9, matrix is 90 with MCP zero rows, carrier is two-value, and all pre-F-55/F-56 migration checksums remain unchanged.
- [ ] **Step 4: Make one joint atomic schema commit.** Do **not** execute the five child commit commands. Stage exactly the union of files listed by Local-AI Task 2 Step 7, MCP Task 2 Step 8, ServerAdmin Task 1 Step 7, Task 2 Step 7 and Task 5 Step 7, then commit once as `feat(f55): land atomic schema and shared identities`. This single commit satisfies those five child commit checkboxes and must be buildable by itself; rerun Step 3 from the committed tree. Only then may the complete `V20261024...` set be applied to a shared development database that has passed `PreF55DatabaseAdmissionV1` and contains the entire `<20261024090000` global chain through `V20261023092800`.

### Task 4: Wire the three feature lines and implement their gates

**Files:**
- Reference: Local-AI Tasks 3–7
- Reference: MCP Tasks 3–7
- Reference: ServerAdmin/carrier Tasks 3–4 and Task 6
- Modify through the referenced AI plan: `scripts/verify-connection-budget.ps1`
- Test: `tests/ai_containment/`
- Test: `tests/mcp_conformance/`
- Test: `tests/mcp_containment/`
- Test: `tests/server_admin/`
- Test: `tests/deployment_carrier/`

**Interfaces:**
- Consumes: complete Task 3 graph, F-56 exact `ModuleLicenseQuery`/entitlement/status/module-lifecycle contract, exact five permissions/bindings, shared receipt ABI, eight-value ClientKind, Stage 14a0 generic evidence/ref/signature/gate-result framework, and Stage 14a1 `F55EntitlementEvidenceQuery`/license-module adapter.
- Produces: AI/MCP runtime/API lines, embedded ServerAdmin SPA, all six F-55 gate implementations/fixtures and their common dependency on the F-56 license/module gate. Stage 14b—not this task—produces real server/carrier/AI resource/license-module certification evidence.

- [ ] **Step 1: Build offline package and application foundations in shared-file order.** Execute Local-AI Task 3 Steps 1–13, then Local-AI Task 5 Steps 1–8, then MCP Task 3 Steps 1–8; each step runs once and each child scoped commit remains authoritative. AI Task 3's CUDA step proves only code/feature compilability here—signed GPU build/SBOM/resource certification belongs to Stage 14b. MCP publication now has both tables, the shared receipt ABI and release framework, so it must not create a second DTO or registry.
- [ ] **Step 2: Wire services and protocol paths in the only conflict-free order.** Execute Local-AI Task 4 Steps 1–9 → Local-AI Task 6 Steps 1–8 → MCP Task 4 Steps 1–11 → MCP Task 5 Steps 1–9 → MCP Task 6 Steps 1–9 → ServerAdmin/carrier Task 3 Steps 1–9. These steps jointly consume F-56 Task 6 exactly once: both AI endpoints call only `EntitlementCodeV1::F55LocalAi`, MCP enable/grant/inbound/outbound calls only `EntitlementCodeV1::F55Mcp`, and ServerAdmin consumes only the F-56 multipart/bootstrap contract. AI/MCP wiring 不得注册虚构 module code 或把 capability entitlement 填进 15 行 module registry；只有具体业务 dataset/object 的调用复用其真实 owner `ModuleCode` gate。They must not define a second license payload/parser/status map or rerun F-56 Tasks 1–5. This serializes shared core/audit/wiring and `mcp_exchange` files；MCP audit fixtures see the already registered AI audit object/actions，MCP local transport sees the carrier/nested validator interface，and the SPA consumes existing AI/MCP/F-56 APIs rather than inventing future DTOs.
- [ ] **Step 3: Implement the seven candidate gate predicates in shared release-registry order.** First consume the F-56 Task 7 predicate `RG-LICENSE-MODULE-LIFECYCLE-GREEN` without duplicating its registry or evidence schema. Execute Local-AI Task 7 Steps 1–7; in Step 8 run only containment/catalog/SBOM fixture checks and assert missing real signed resource report keeps `RG-AI-RESOURCE-CERTIFIED` nonzero, then perform Step 9. Execute MCP Task 7 Steps 1–9. Execute ServerAdmin/carrier Task 6 Steps 1–9, 11 and 12, never Step 10; then execute ServerAdmin/carrier Task 4 Steps 1–7 last because its browser/matrix fixtures consume all preceding read models and candidate conclusions. Candidate fixtures may prove parser/predicate behavior but cannot be promoted to a release PASS; real license/module, AI resource and two-carrier runs belong only to Stage 14b.
- [ ] **Step 4: Verify only the cross-line catalog, roster, and budget aggregates.** Do not repeat any child-plan focused test, the F-56 plan's focused suite or the three line acceptances owned by Task 5 Step 6. Run exactly `cargo xtask errorcodes && cargo xtask configdoc`, `cargo test -p ep-xtask --test f55_service_roster`, and `powershell -NoProfile -File scripts/verify-connection-budget.ps1`. Expected: AI 11/config 7/metrics 7, MCP 20/config 9/metrics 7、carrier 4/metric 1 and F-56 PLATFORM 6/metrics 3 consume their single catalog registrations without duplicates；global totals are 495 errors and 70 metrics；nine product processes/PEs/services, eight resource units, six no-SQL selfcheck scopes, all-zero `ai-inferer` DB row and total `37/10/5/52` agree. Real Stage 14-only evidence remains absent/nonzero, never a false PASS.

### Task 5: Prove cross-line invariants on a fresh database

**Files:**
- Create: `testkit/tests/f55_integration_pg.rs`
- Modify: `xtask/src/ci.rs`
- Modify: `scripts/verify-release.ps1`
- Test: `testkit/tests/f55_integration_pg.rs`

**Interfaces:**
- Consumes: all nine migrations, eight-value `ClientKind`, AI/MCP routes, embedded ServerAdmin assets and deployment carrier facts.
- Produces: one `f55_integration_pg` gate proving the release cannot contain a partially merged F-55 graph.

- [ ] **Step 1: Write the failing integration test.** 测试先把 migration catalog 的九个 F-55 path 与 `db/migrations/` 实际文件做 exact-set 比较，要求九个文件全部存在、非空、文件名/owner/version 唯一且没有额外 `20261024090000..=20261024090800` 文件；随后创建 fresh PostgreSQL 16 database，先执行并断言 Stage 14 §0.0 的 `PreF55DatabaseAdmissionV1` 六项：从零应用所有 `<20261024090000` 的全局迁移、Stage 14 自有 28 文件末项恰为 `V20261023092600`、全局 pre-F55 末项恰为 `V20261023092800`。只有 admission 绿色后才在同库接续九个 F-55 迁移，并断言下列精确 tuple：

```rust
assert_eq!(facts.product_process_count, 9);
assert_eq!(facts.client_kind_count, 8);
assert_eq!(facts.audit_client_value_count, 9);
assert_eq!(facts.capability_matrix_cell_count, 90);
assert_eq!(facts.f55_migration_count, 9);
assert_eq!(facts.mcp_method_count, 6);
assert_eq!(facts.ai_containment_assertion_count, 9);
assert_eq!(facts.deployment_carrier_count, 2);
assert_eq!(facts.ai_pipe_instance_count, 51);
assert_eq!(facts.ai_compose_route_slots, 45);
assert_eq!(facts.ai_compose_timeouts_ms, (120000, 122000));
assert_eq!(facts.ai_runtime_abi_version, 1);
assert_eq!(facts.ai_runtime_tuple, ("CANDLE", "0.11.0", "HF_TOKENIZERS_RUST", "0.23.1"));
assert_eq!(facts.ai_model_tuple, ("QWEN2", "GGUF_V3", "MOSTLY_Q4_0"));
assert_eq!(facts.ai_decoder_tuple, ("GREEDY_SCHEMA_DFA_V1", 2048, &[151645]));
assert_eq!(facts.f55_permission_seed_count, 5);
assert_eq!(facts.f55_scope_binding_seed_count, 5);
assert_eq!(facts.f55_seeded_role_grant_count, 0);
assert_eq!(facts.mcp_exchange_stream_abi, "McpExchangeChunkStreamV1");
assert_eq!(facts.mcp_exchange_frame_variant_count, 9);
assert_eq!(facts.mcp_exchange_ack_states, &["CONTINUE", "COMPLETE", "ABORTED"]);
assert_eq!(facts.mcp_terminal_json_bytes_limit, 8_388_608);
assert_eq!(facts.mcp_sse_decoded_body_bytes_limit, 8_388_631);
assert_eq!(facts.mcp_route_slots, (16, 4));
assert_eq!(facts.mcp_route_timeouts_ms, (30000, 32000));
assert_eq!(facts.mcp_local_transport_count, 2);
assert_eq!(facts.mcp_sandbox_persistence_column_count, 10);
assert_eq!(facts.mcp_wfp_filter_key_count, 6);
assert_eq!(facts.mcp_rate_owner_count, 3);
assert_eq!(facts.mcp_audit_spool_owner_count, 2);
assert_eq!(facts.mcp_audit_spool_slots_per_owner, 1_024);
assert_eq!(facts.mcp_audit_spool_jcs_bytes_limit, 1_048_507);
assert_eq!(facts.high_risk_operation_count, 7);
assert_eq!(facts.mcp_remote_exchange_ace_count, 2);
assert_eq!(facts.mcp_local_exchange_ace_count, 2);
```

The same integration test must prove that the only license path is F-56 `ModuleLicenseQuery`: one signed current/history grant supplies `F55LocalAi|F55Mcp`, `Active|ExpiringSoon|GracePeriod` is currently licensed, historical CRL-isolated grants never count as purchased, purchased never substitutes for currently licensed, and config/module/feature/environment/manual evidence cannot synthesize entitlement. The no-current/empty-table fixture must consume the Stage 14 exact shape `grants=[]/current_grant=None/RESTRICTED/NO_CURRENT_GRANT` and all four purchase/current booleans false; it may not invent an empty current object. It must separately exercise actual owner-module effective-runtime gates without registering an AI/MCP pseudo-module: a missing/tampered entitlement stops the platform capability, while `SIGNER_REVOKED|INVALID` on the concrete business object's current owner module stops that object's AI/MCP path before payload/inference/egress without rewriting deployment `LicenseStatus`; unrelated module failure cannot globally relabel AI/MCP. Under a globally `Restricted` but structurally trusted current grant, AI pure read/draft and every `ReadReportAuditBackupExport` path remain usable; ordinary business write/approval, side-effecting MCP outbound or dynamic `Write|Approve`, egress and new automation return exact `PLATFORM.LICENSE.RESTRICTED`. Only the frozen `LICENSE_GRANT` recovery chain and `MODULE_PACKAGE:DISABLE` chain bypass those restricted side-effect classes; every other module action, ordinary config package and ServerAdmin approval conclusion remains blocked. The same fixture must assert ServerAdmin's 15 module rows expose exact `package_trust_status=NOT_INSTALLED|TRUSTED|SIGNER_REVOKED|INVALID` independently of install state.

It must also prove that `mcp` has zero matrix rows, `ai-inferer` owns zero DB credentials, ServerAdmin adds zero listeners, permission ids `...0310`–`...0314` and binding ids `...0504`–`...0508` match the five exact F-55 rows, and remote/local exchange ACEs are both exactly core+worker while adapters gain no audit/DB authority. Every MCP direction closes the caller-owned audit state machine through the exact core/worker 1-GiB dedicated spool grammar/recovery, and pure-system outbound jobs stop before rate/attempt/dispatch. Assert exact connector edges and locked DISABLED→ENABLED prerequisites, exact terminal grant edges/scanner/revoke/final-counter transitions, and optional grant limit defaults/bounds from the two exact logical config keys. Assert the nine MCP frame tags/fields including matching invocation id and no-ACK `DispatchAuthorized`, independent request/response sequences, RequestEnd COMPLETE→caller deterministic ATTEMPT→authorization→single dispatch, raw-only ResponseEnd COMPLETE→caller size-first schema/field validation with no ACK reversal/refund/replay, exact ACK/abort positions/one-Abort close, local-monotonic non-resetting deadline, raw response rather than wrapper bytes, terminal/SSE size-before-schema limits, the sole bounded request `_meta` exception and two exact inbound error-data shapes. Assert caller dedicated-connection shared-lock double reads, same-key exclusive state mutation, zero receiver current-connector reads, `max_calls=1` accepted-to-CONSUMED first-call dispatch, full fifteen-column LOCAL materialization group, exact three-field receipt ACK/replay semantics, type-dependent receipt manifest digest, canonical signature strings, artifact wire-kind/MIME closures, six WFP filter keys/layers and exact WFP SD rather than two-key/numeric-id persistence, per-version nonshared stdio roots, fixed `package.cab+entries` roster and CAB/entry equality, signed stdio LPAC/NUL-stderr/WER readback, strict Hyper-V OCI/Authenticode/base allowlist/scratch teardown and the rule that ordinary HTTP routes cannot call outbound exchange. Assert core/gateway/plugin-host are the three unsplit rate owners, cold start seeds 60 timestamps, accepted reservations are not refunded after the frozen boundary, and proof-counter accepted response headers follow counter acceptance rather than HTTP success—including accepted-header + zero-dispatch on inbound ATTEMPT failure. CPU/GPU resolved feature facts both contain the frozen tokenizers/onig closure, GPU alone additionally contains Candle `cuda`; `AiRuntimeReleaseFactsV1` is in-memory only and its digest is bound into the final signed report for the same release batch. Assert AI model status/five-time truth, exact reporting per-route errors/confirmed behavior and cursor/page_size-only AI admin pagination. Carrier assertions include policy SPKI anchors, run-bound nonce, AK certificate chain, quote/PCR/event-log replay and canonical authorization/sidecar signature strings. The MCP exchange ABI neither imports nor aliases Stage 3 `BoundedChunkStreamV1`.

- [ ] **Step 2: Run the test before CI wiring.** Run: `cargo test -p ep-testkit --test f55_integration_pg -- --nocapture`. Expected: FAIL because the aggregate facts/gate are not yet registered.
- [ ] **Step 3: Register the integrated gate.** Add `f55_integration_pg` to the Windows/PostgreSQL portion of `cargo xtask ci`; make `scripts/verify-release.ps1` evaluate seven logical gate slots—the common F-56 `RG-LICENSE-MODULE-LIFECYCLE-GREEN` plus the six F-55 codes—without counting loose files or seven detached digests. The common slot and every `RequiredPass` slot must strict-parse and verify the exact Stage 14 `evidence-index.v1.jcs`、`gate-result.v1.jcs` and both adjacent `Stage14EvidenceSignatureV1` sidecars under that gate's fixed root, including exact code/outcome/path/ref/digest, typed entry roster, same `stage14_run_id/deployment_id/product_build_sha256` and closed-window binding. Common lifecycle parser 还必须要求其 `deployment_manifest_evidence_ref/sha256` 与 `initial_governance_evidence_ref/sha256` 两个必填 child：前者核独立 deployment CMS 与 `DEPLOYMENT_MANIFEST_EVIDENCE_V1` sidecar，后者核无 sidecar 的 Stage 14 typed initial-governance evidence；两者均从固定 resolver 解析并同 run/deployment/build/window，不能变成第五个 common top-level code。Every `DisabledEvidence|NotInBuild` slot must prove the corresponding final index/result/sidecars are all absent; a forbidden result or orphan index/sidecar is a hard failure.
- [ ] **Step 4: Run fresh-database validation.** Run: `cargo xtask sqlcheck && cargo test -p ep-testkit --test f55_integration_pg -- --nocapture`. Expected: `PreF55DatabaseAdmissionV1` first passes on the complete `<20261024090000` chain ending at `V20261023092800`; the same fresh database then applies all nine F-55 migrations and passes the exact tuple above with zero historical migration checksum changes.
- [ ] **Step 5: Commit the cross-line gate.** Run: `git add testkit/tests/f55_integration_pg.rs xtask/src/ci.rs scripts/verify-release.ps1 && git commit -m "test(f55): enforce integrated release invariants"`.
- [ ] **Step 6: Run the three line acceptances once, without re-executing child checkboxes or Task 4 aggregates.** AI: `cargo test -p ep-testkit --test f55_ai_schema_pg && cargo test -p ep-app-reporting --test f55_ai_query_plan && cargo test -p core-server --test f55_ai_http && cargo test -p ep-xtask --test f55_ai_openapi && cargo test -p ep-ai-containment`. MCP: `cargo test -p ep-testkit --test f55_mcp_schema_pg && cargo test -p ep-platform-mcp --test f55_manifest && cargo test -p ep-adapter-ipc --test f55_mcp_exchange_chunk_stream_windows --target x86_64-pc-windows-msvc && cargo test -p core-server --test f55_mcp_inbound && cargo test -p integration-gateway --test f55_mcp_remote && cargo test -p plugin-host --test f55_mcp_local && cargo test -p ep-xtask --test f55_mcp_openapi && cargo test -p ep-mcp-conformance && cargo test -p ep-mcp-containment`. ServerAdmin/carrier: `cargo test -p ep-testkit --test f55_client_kind_schema_pg && cargo test -p ep-testkit --test f55_server_admin_matrix_pg && cargo test -p ep-testkit --test f55_deployment_carrier_schema_pg && cargo test -p core-server --test f55_server_admin_assets && cargo test -p core-server --test f55_ai_model_package_view && cargo test -p ep-xtask --test f55_ai_admin_openapi && npm --prefix tests/server_admin test && cargo test -p ep-deployment-carrier && cargo run -p ep-release-gate -- verify --gate RG-SERVER-ADMIN-MATRIX-90-GREEN --evidence-dir target/release-evidence`. Expected: all implementation/fixture gates pass; only real Stage 14b evidence gates remain nonzero.

### Task 6: Bind F-55 gates for Stage 14b without weakening disabled mode

**Files:**
- Modify: `xtask/src/ci.rs`
- Modify: `scripts/verify-release.ps1`
- Create: `tests/f55_disabled_mode/Cargo.toml`
- Create: `tests/f55_disabled_mode/src/lib.rs`
- Create: `tests/f55_disabled_mode/tests/f55_disabled_mode.rs`
- Test: `tests/f55_disabled_mode/tests/f55_disabled_mode.rs`

**Interfaces:**
- Consumes: Stage 14a0 的 fail-closed 十五值 gate registry、`Stage14GateEvidenceIndexV1`/`Stage14GateResultV1`/两项 gate sidecar purpose、六项 F-55 gate，以及 Stage 14a1 接入的 F-56 `RG-LICENSE-MODULE-LIFECYCLE-GREEN`/signed-grant 四态与全部模块 enable flags。
- Produces: 交给 Stage 14b 的七个逻辑 gate slot applicability predicate、禁用态套件与 typed candidate evidence；购买并启用的能力必须通过对应 F-55 gate 以及共同 F-56 gate。配置停用、entitlement 缺失/失信或 module effective runtime 不可用时无对应业务 route/inference/egress/listener/child process且保留全部登记数据；全局 `Restricted` 则只关闭有副作用的 admission effect，不能误关 AI 纯读取/草稿或 `ReadReportAuditBackupExport`。13c 不生成任何 final gate index/result；只有 Stage 14b 可把候选 evidence 原子组装、签名为最终发布证据。

- [ ] **Step 1: Write disabled-mode failures first (executed early by Task 2 Step 4).** Create package `ep-f55-disabled-mode`. Add fixtures with `EP__AI__ENABLED=false`, `EP__MCP__INBOUND_ENABLED=false`, `EP__MCP__OUTBOUND_ENABLED=false`; assert AI/MCP business routes are absent, `ep-ai` refuses compose, gateway has no MCP egress, plugin-host has no MCP child, and all model/connector/manifest/grant/license/module/config-package rows remain readable by authorized admin queries. Add table-driven F-56 fixtures for current signed grant status `Active|ExpiringSoon|GracePeriod|Restricted`、zero-current/empty-table、absent/tampered grant and four `package_trust_status` values: the first three proceed only with exact entitlement and feature flag，and concrete business-object operations additionally require that object's real owner module to be `TRUSTED`；zero-current/empty-table exact output is `grants=[]/current_grant=None/RESTRICTED/NO_CURRENT_GRANT` with all four booleans false；Restricted keeps AI pure read/draft and `ReadReportAuditBackupExport` available but rejects write/approve/side-effecting MCP outbound/new automation with exact platform code；absent/tampered grant closes the capability before payload/inference/egress，while `SIGNER_REVOKED|INVALID` only on the concrete owner module closes that object's path；all rows remain preserved and `NOT_INSTALLED` cannot be treated as trusted。This checkbox is completed during Task 2 and is not repeated here.
- [ ] **Step 2: Observe the disabled-mode red state (executed early by Task 2 Step 4).** Run once: `cargo test -p ep-f55-disabled-mode`. Expected: FAIL until all routing and process conditions are wired. Preserve this evidence; do not rerun it as a second “before” step.
- [ ] **Step 3: Bind the seven exact gate predicates.** `RG-LICENSE-MODULE-LIFECYCLE-GREEN` verifies the one F-56 signed-grant/module-package chain and is the common applicability prerequisite for either AI or MCP; its typed evidence roster is exact four: `license_module_lifecycle_matrix|license_admission_registry_exact_set|license_admission_negative_matrix|license_trust_rotation_exact_set`，分别覆盖 F-56 §8 前十组生命周期、`LicenseAdmissionGate` HTTP/MCP/core/worker registry exact-set 与 guard/bypass 负例、以及全部 RELEASED special item/accepted bundle/current license/current module/CRL isolation exact-set；不得留下自由格式第五类 evidence。trust entry 必须按 Stage 14 exact domain/JCS ABI 重算 source/current/module/CRL/containment/negative digest，并逐项核对 immutable accepted bundle exact bytes、`platform.config_special.accepted.v1` closed payload、append-only audit hash chain 与 inner/outer chain digest；正确 `HISTORICAL_SIGNER_REVOKED` containment 不计正向能力但不得被误判为 FAIL。`license_module_lifecycle_matrix` 还必须按 Stage 14 exact ABI 解析两个必填 child：`deployment_manifest_evidence_ref/sha256` 命中已验签独立 deployment CMS、产品保护的 `deployment-roots.p7b`、管理员证书 roster、双 recipient X509 bundle readback 与 `DEPLOYMENT_MANIFEST_EVIDENCE_V1` sidecar；`initial_governance_evidence_ref/sha256` 命中 deployment-id 子目录下无 sidecar receipt、审计/DB/首张 grant/ACTIVE key-domain/两名 password+X509 MFA 的 typed evidence。两个 child 只由 lifecycle digest 传递绑定，不能增加 common top-level code。This plan consumes the common gate's Stage 14 ABI and never redefines its evidence DTO. `RG-AI-CONTAINMENT-GREEN` requires the exact nine named containment assertions; `RG-AI-RESOURCE-CERTIFIED` requires the signed Stage 14b joint-load/resource report; enabled and F-56-purchased AI requires all three. `RG-MCP-CONFORMANCE-GREEN` requires the pinned six-method positives and every forbidden-method negative; `RG-MCP-CONTAINMENT-GREEN` requires grant/manifest/gateway/plugin-host/high-risk/credential/egress containment; either enabled MCP direction with F-56 purchase requires these two plus the common gate. Every build containing ServerAdmin requires `RG-SERVER-ADMIN-MATRIX-90-GREEN`; every deployment requires `RG-DEPLOYMENT-CARRIER-EVIDENCE-GREEN` for the selected carrier. For the common slot and every `RequiredPass`, verification requires the fixed-root exact index/result/two sidecars, outcome `PASS`, typed entry resolution and same run/deployment/build/closed window; missing, malformed, orphaned or digest/signature/ref-mismatched evidence is a hard release failure. `purchased` 只从 `TRUSTED` current/history F-56 entitlement 重算，排除 `HISTORICAL_SIGNER_REVOKED`；`currently_licensed` 另要求 current signed grant scope 和 `Active|ExpiringSoon|GracePeriod`。AI/MCP not purchased or disabled follows `DisabledEvidence`, while absent ServerAdmin follows `NotInBuild`; those branches forbid a final result and orphan index/sidecar and are accepted only after disabled-mode proves business routes absent、pre-payload fail-closed、zero inference/egress/local child process and retained registrations。全局 Restricted 是按 effect 裁切的 retained-data runtime state：它拒绝副作用但保留 AI 纯读取/草稿与 ReadReportAuditBackupExport，并且永不伪造 gate `PASS`。Health/control service surfaces allowed by the common installation baseline are not business capability. The disabled-mode suite itself is mandatory for every base-product release；本计划不新造 gate outcome 枚举或 wire token。
- [ ] **Step 4: Turn the preserved disabled-mode suite green and verify candidate applicability.** Run: `cargo test -p ep-f55-disabled-mode && cargo test -p ep-testkit --test f55_integration_pg -- --nocapture`. Expected: PASS for disabled/restricted routing, signed-entitlement truth, data retention and candidate predicate logic; additionally run all seven logical real-evidence slots, including `RG-LICENSE-MODULE-LIFECYCLE-GREEN`, with an empty evidence directory and assert nonzero. Add malformed candidate fixtures for wrong code, non-PASS/unknown outcome, missing index/result/sidecar, orphan index, bad index ref/digest/signature, raw path, reparse/ADS/hardlink, cross-run/build/deployment/window evidence and forbidden result under `DisabledEvidence|NotInBuild`; common lifecycle fixtures还须覆盖缺 deployment/initial child、坏 deployment CMS/`DEPLOYMENT_MANIFEST_EVIDENCE_V1` sidecar、receipt 出现禁用 sidecar、source 路径 deployment id 不等、ep-migrate/core X509 bundle digest 不等、same-SPKI/different-DER、错 SKI、password-only/X509-only、首张 grant/key-domain/audit 断链。每个负例都保持非零，证明 fixture success 不能授权发布。
- [ ] **Step 6: Commit the Stage 13c release-closure candidate before handoff.** Run: `git add xtask/src/ci.rs scripts/verify-release.ps1 tests/f55_disabled_mode && git commit -m "test(release): close F-55 gates"`. This is the final Stage 13c action and must produce the immutable candidate consumed by Stage 14b; it does not claim any real-evidence gate PASS.
**External Step 5: Stage 14b acceptance after Step 6; not a Stage 13c checkbox.** Only after the committed Stage 13c candidate and all other Stage 14b prerequisites exist, run on the Windows Server 2022 release agent: `cargo xtask ci` followed by `powershell -NoProfile -File scripts/verify-release.ps1`. Expected: PASS only when the common `RG-LICENSE-MODULE-LIFECYCLE-GREEN` and every applicable F-55 slot have their exact `Stage14GateEvidenceIndexV1`、`Stage14GateResultV1` and two valid sidecars, each outcome is `PASS`, each typed evidence roster resolves, and all pairs bind the same exact `stage14_run_id/deployment_id/product_build_sha256` and closed window; `DisabledEvidence|NotInBuild` slots instead have no final pair or orphan sidecar and are backed by the mandatory signed disabled/restricted-mode report. The common index must include exact `license_module_lifecycle_matrix|license_admission_registry_exact_set|license_admission_negative_matrix|license_trust_rotation_exact_set` four-entry ABI，逐项解析 Stage 14 冻结 DTO/ref/case/chunk，且从 lifecycle manifest 继续解析 exact deployment-manifest 与 initial-governance 两个 child，核对它们的 CMS/sidecar/DACL/source/ref、首装无 receipt sidecar、双 X509/MFA、current license 与 current module 独立结论；两个 child 不是第五/第六个顶层 entry。The numeric label is retained for existing cross-references; the master DAG explicitly executes this external continuation after Step 6, so no executor may treat checkbox numbering as permission to run it before the candidate commit. Before Stage 14b this command must remain nonzero, no partial pair may be accepted, and no package may be released.

## Integration Exit Checklist

- [ ] 九个 F-55 迁移一次完整 fresh-database 执行，历史迁移 checksum 零变化。
- [ ] F-56 的 Stage 3=18/Stage 13=20 item-kind 顺序、signed current grant 四态、`F55LocalAi|F55Mcp` 唯一 entitlement 来源、五条声明式模块动作、`ModuleOperationGate` 与共同 `RG-LICENSE-MODULE-LIFECYCLE-GREEN` 已作为前置和 applicability 依赖验收；第 2 节 73/全局 495 与 F-56 六码精确，普通业务 Restricted 用 `PLATFORM.LICENSE.RESTRICTED`，恢复例外只含 `LICENSE_GRANT|MODULE_PACKAGE:DISABLE` 两条冻结全链；13c 未建立第二套许可 payload/parser/status/gate。
- [ ] `ClientKind` 八值、audit 九值、90 格矩阵、MCP 非矩阵例外在 Rust、SQL、OpenAPI、指标和测试中逐项一致。
- [ ] 产品常驻进程、安装包、Windows 服务登记、资源单位与连接预算均采用 F-55 终态：九个产品进程与九份产品 PE、`deploy/register-services.ps1` 九个产品服务行、八个产品资源单位；`scripts/verify-connection-budget.ps1` 恰有九行且 `ai-inferer` 五项连接计数全为 0，总预算仍为 `37/10/5/52`。
- [ ] AI single-CAB/fixed `package.cab+entries` install receipt→DB 登记→exact 1-MiB resource report/canonical `signature_p1363_b64url` sidecar/不透明 ref→独立 activate ACK→compose→confirm→execute 全链成立；跨表 receipt UUID、exact 三字段 ACK/replay、模型五时间/状态 truth table、重启/断连/DB-ACK/认证事实不一致均 fail-closed。运行取消 2,000 ms 未确认时必须终止整个 `ai-inferer`/`APP_AI` Job，全部同进程在途请求取消/清零/NOT_ACTIVE，重启并完成 fresh package verification/activate ACK/certification gate 后才重开；测试证明无 per-invocation 子进程、旧 ACK 或迟到结果。P-256 法人 plan token 与每请求 KV 隔离通过，exact AI audit object/actions/strict after 和九条断言名称精确，结果进入模型链路的字节数为零。Compose/execute 是仅有的只读 POST 幂等豁免、都拒绝 Idempotency-Key 且零幂等表读写；compose-only NOT_VISIBLE 与 execute token-invalid/current-context-changed 的 per-route 闭集、required ordinary `confirmed` 已按 OpenAPI exact 比对。Execute 响应超 8,388,608 bytes 时丢弃全部结果并返回 `REPORTING.ANALYTIC_QUERY.RESULT_TOO_LARGE`，绝不用 `truncated` 掩盖字节截断。
- [ ] AI Runtime ABI 恰为 Candle `0.11.0/0.11.0` + Rust tokenizers `0.23.1`/onig + QWEN2/GGUF v3/MOSTLY_Q4_0 固定 tensor roster + 固定 DFA/EOS；CPU/GPU 签名、feature、Cargo.lock、vendor、SBOM 和认证报告逐项闭合，`AiRuntimeReleaseFactsV1` 只当场重算并以 digest 进入最终报告，不形成第二套证据；locale 只接受 `zh-CN`。
- [ ] MCP 六应用方法（含 `resources/templates/list`）、literal/template 分离、stateless headers、list freshness hints、唯一有界 request `_meta` extension map、两种 exact inbound error data、入站即时 JSON 与出站 request-scoped SSE 正例全部通过；ping/initialize/application-cancel/inbound SSE/legacy GET-SSE、所有禁用 capability、exact 50-byte grant token、optional TTL/call limits 受当前 600/100-downward config 约束、grant 仅 ACTIVE→三个终态、`max_calls=1` accepted-to-CONSUMED first dispatch、connector exact edges/locked enable prerequisites、七个 `HighRiskOperation`（含 `DATA_MIGRATION`）、合同终止和审批动作负例全部通过；device PoP/受理 counter header、不退还的唯一 owner rate、manifest trust bundle/canonical signature string/轮换、transport-shaped 单 credential（Hyper-V 必须 None）、rollback-by-copy、exact artifact kind/MIME closure、LOCAL 十五列 receipt/materialization 一次补全、MCP receipt connector-manifest digest、九帧双向 `McpExchangeChunkStreamV1`、RequestEnd COMPLETE→ATTEMPT→无 ACK DispatchAuthorized、raw-only ResponseEnd COMPLETE→caller schema validation与 CONTINUE/COMPLETE/ABORTED、raw 8,388,608-byte terminal/8,388,631-byte SSE 上限、caller shared-lock双读/same-key exclusive mutation/zero-DB receiver、16→4/30-in-32 秒 route gate、core/worker remote+local ACE、人类来源 job、core/worker 专用 spool exact reserve/tmp/ready/corrupt 恢复、`MCP.AUDIT.UNAVAILABLE`、全方向 exact phase/request/binding/completion audit truth、single-CAB/fixed-root roster、每版 stdio 根、AppContainer/LPAC/六层 keyed-WFP exact SD/NUL-stderr/WER、strict Hyper-V OCI/scratch 和 local-only cancellation 证据完整。
- [ ] `integration-gateway` 仍为 0 DB/KMS/business-file/Outbox，`plugin-host` 子进程无网络/任意文件/child process，ServerAdmin 无新增进程/端口。
- [ ] 物理机与境内 IaaS VM 使用同一制品和判据；统一 bounded strict-JCS policy/evidence/child/probe、UUIDv7 Stage 14 run、两类 TPM 2.0、policy vTPM SPKI trust anchors、VM run-bound nonce/AK cert chain/quote/PCR/bounded event-log replay、客户控制、条件 nested、三份 backup probe/bundle/ref/digest、双人授权与 deployment-KMS canonical `signature_p1363_b64url` sidecar 全部闭合。
- [ ] 配置禁用、entitlement 缺失/失信或 module effective runtime 不可用时没有隐藏业务监听、推理、出网或子进程；全局 F-56 `Restricted` 只关闭写/批/有副作用外发/新自动化，AI 纯读取/草稿与 `ReadReportAuditBackupExport` 仍可用；两类分支都安全保留 AI/MCP/许可/模块数据并返回各自冻结错误。
- [ ] `Stage 1/2 → Stage 14a0` 与 `F-56 Stage 3b-2` 两支可并行，并在 Stage 14a1-F56-adapter 汇合后才进入 13c entitlement wiring；F-56 Stage 13b 与 13c 其余依赖按本计划汇合后再单向进入 Stage 14b。13c 只交付实现、夹具和 typed candidate evidence；共同 F-56 gate 与每个 `RequiredPass` F-55 gate 的 exact signed index/result、最终判定及发布许可只发生在 Stage 14b，`DisabledEvidence|NotInBuild` 不生成 final pair，任何前置/13c 均无部分发布路径。
- [ ] OCR、向量/RAG、工业协议、外部 AI、SaaS、HA、Kubernetes、托管 DB/KMS、Excel 加载项不可达；XLSX/CSV 正例仍通过。
