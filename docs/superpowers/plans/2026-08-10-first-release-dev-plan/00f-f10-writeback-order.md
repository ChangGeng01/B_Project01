# F-10 回写工单（2026-08-20）——**已获授权，第一批已执行**

> `status: SUPERSEDED_FOR_IMPLEMENTATION`
> `replaced_by: F-50`
> `normative: false`
> 本工单不是现行队列，正文的“待拍板、扣住、未做”等词只记录当时状态。

> **F-48/F-50 后续状态（2026-08-21）。** 本工单记录 F-10 当时的批次与失败原因，不再是当前执行队列。B-2 三来源由 F-48 改为四来源；B-3 至 B-8 的核销方向、退款、动态资金冲正、释放公式、有效余额、历史切片、发票头行/号码及历史成交由 F-50 替代。当前剩余动作只按 F-50 实施计划顺序执行；下文“扣住/未做/REFUND/min/逐行取反”均为历史状态。

## 当前 supersession 台账

| 原工单范围 | 当前状态 | 唯一依据 |
|---|---|---|
| B-2 凭证来源 | 已由 F-48 关闭，固定四类生成来源 | `00c-gap-ruling.md` F-48 |
| B-3 至 B-8 被替代财务与发票句 | 已由 F-50 关闭，不再续跑本工单旧 patch | F-50 设计 §2—§10 |
| F-49 九项 | 全部关闭 | `00c-gap-ruling.md` F-50 |
| 开发执行顺序 | 已迁移到单一计划 | `2026-08-21-f50-financial-consistency-implementation.md` |

## 执行台账（2026-08-20 第一批）

使用方已授权回写。本批**落 28 条、扣 9 条、因缺逐字或待裁而未生成 34 条**。

### 已落 28 条

`S-01` `S-02`（规格 288／874，A-1+A-5）、`P-01`（PRD 新增 3.5.5 影响面处置台账）、
`P-02`…`P-05` `P-07` `P-08` `P-09`（A-4 词义、A-5 状态机、A-7、A-12 谓词扩面）、
`P-10` `P-11` `P-12`（B-5 台账改追加式）、`S-10+S-11` `P-40`（B-1 更正凭证 + B-2 凭证来源封闭）、
`P-46` `P-48`…`P-52` `S-13` `S-14` `S-15`（C-3／C-1／C-2）、
`S-17` `S-20` `P-53` `P-54`（D-1 治理半、D-3 表述权限与诚实披露）。

落盘纪律：每条落前断言其 `old` 串在目标文件中**恰好命中 1 次**，不唯一即整批不落。
落后体检十条禁写措辞（`WAIVED`、`approval_ref`、SHA-256 冻结、快照哈希不一致、
`configurable-transition-disjoint`、`RG-TASK-ECONOMY` 等）在规格与 PRD 中**各命中 0**；
`碾压` 在规格命中 1，即 21.22 节自身的禁用词清单，属正确。

### 扣住 9 条：`S-04`…`S-09`、`P-24` `P-25` `P-27`

**理由是第 4 节风险五，且实测比该节所述更严重——矛盾是跨文档的。**

| 组 | 已生成 | 缺 |
|---|---|---|
| B-3 + B-6 | 7/17（规格侧齐，PRD 侧几乎全缺） | `P-14`…`P-23` |
| B-4 + B-8 | 2/13 | `P-26`…`P-37` |
| B-1 + B-2 | **齐** | —（`S-10+S-11` 一条承接规格两半，`P-40` 一句承接 PRD 两半） |

**一个实的例子说明为什么不能只落规格侧**：若落 `S-09`（规格允许分次部分红冲）而 `P-28` 未落，
`PRD:2417` 仍逐字「只能选择状态为**已开具**的销项发票」——**第二次部分红冲在 PRD 层被堵死，
规格说能、PRD 说不能。** 这正是「自相矛盾的半句」，只是跨在两份文档之间。

### 未生成 34 条：主因是**工单没给逐字文本**

`P-14`…`P-23`、`P-26` `P-28`…`P-37`、`P-41` 等，工单只给了语义提要（如「改为条件式」
「各改为两个产生方式」「必填改条件必填」），**没有可直接落盘的句子**。
按纪律「工单没给逐字的不要自己编」，一律标 blocked。
**这不是遗漏，是这批回写的真实完成度**——补齐逐字后可直接续落。

### 第二批（2026-08-20）：起草 23 条，对抗核驳回 10 条，落 4 条

使用方指示「全面完成」，本轮为缺逐字的条目起草可落盘文本。**结果是这批没能全面完成，如实记。**

| | |
|---|---|
| 起草 | 23 条 |
| 对抗核通过 | 13 条 |
| **对抗核驳回** | **10 条** |
| 起草时即 blocked | 7 条 |
| **实际落盘** | **4 条**（`S-16` `S-18` `S-21` `P-41`） |

**为什么通过 13 条却只落 4 条**：`B-3+B-6` 与 `B-4+B-8` 两组仍凑不齐，
而工单第 4 节风险五明写这两组不可拆。其中 `P-18`／`P-19`／`P-20` 工单逐字「必须同批」——
`P-19` 通过而 `P-18`、`P-20` 被驳，故 `P-19` 也不能单独落。
落的 4 条都不在任何不可拆组内：`S-16`（规格 13.3 保留期前提）、`S-18`（第 22 章第 13 条恢复文档）、
`S-21`（21.4 新增第四类签字对象「对外表述与宣传材料」）、`P-41`（B-1 更正凭证字段级细化，其主体已在第一批落齐）。

#### 对抗核驳回的 10 条：全部是钱账口径错误，值得逐条留档

这一轮的价值主要在这里——**如果没有这道对抗核，下面这些会写进规格与 PRD。**

- **`P-21`（最重）**：把 B-6 的**四路分支压成「一律转预收」**。
  详本逐字对 `origin = REFUND` 一路写的是「**阻断提交**……提示**先冲正该退款单**（钱已经出去了转不成预收）」，
  而草稿写「**不得**以资金单据冲正解此场景」——**正好写反**。
  落下去会凭空贷记一笔没有资金腿的预收，**借贷两端根本不成立**。
- **`P-15`**：与第一批已落的文本正面冲突。草稿写「已核销金额按相消后的净额计算」，
  而 `6.9.1` 已落逐字「未核销余额等于原单据金额减已核销金额」、`6.5.3` 已落逐字「原条目的未核销余额**一字不动**」。
  代进守恒式：已核销降低 → 未核销必然等额上升，与「一字不动」逐字对撞。
- **`P-17`**：**双计**。把只属于 `MANUAL` 一路的「新增挂账」写成整条路径的通则，
  又在另一句叠加 `AUTO_ADVANCE` 的「回增」，**同一笔释放额既是新挂账又是回增，预收合计可被读成 2× 释放额**。
- **`P-18`／`P-20`**：把「条件必填」写成了「**禁止填**」，且**判别项取错**——
  详本给的判别是「**有没有退货单**」，草稿取成「款项来不来自预收」。
  后果不是文字问题：详本明写退货那道可退上限「是收窄不是删除」，
  按草稿的豁免面，最常见的退货退款反而落进豁免，**退货金额那道封顶当场失效**。
- 其余 `P-30`／`P-31`／`P-32`／`P-36`／`P-37` 各有追不到详本、或与已落文本冲突的问题。

#### 本轮的结论

**「全面完成」这一轮没有做到，卡点不是授权也不是工作量，是两件事：**

1. **钱账条目的起草质量。** 23 条里 10 条被驳，且驳的理由全部成立。
   这类条目要落，得由**懂这套账的人**逐条定稿，或者接受更多轮起草—对抗核循环。
2. **几条真正需要业务决策的前置**（见下节）。

### 第三、四批（2026-08-20）：均落零，四轮数据见 `00h`

| 轮次 | 起草 | 通过 | 通过率 | 落盘 |
|---|---|---|---|---|
| 第三批（带驳回诊断） | 21 | 6 | 29% | **0** |
| 第四批（带 F-46 九条财务裁定） | 30 | 4 | 13% | **0** |

**第四批补齐了全部财务判断之后通过率仍然下降**，这排除了「缺业务决策」与「起草方不懂账」两个解释。

**真实原因：每一轮对抗核都在发现工单没有映射到的依赖。** 四轮累计四处，
**没有一条出现在本工单里**，详见 `00h-b-cluster-blockers.md`：

1. **「非退货退款」实为五处堵，本工单只点了三处。** 第四处（往来方字段）已由 F-46 第三条解掉；
   **第五处 `PRD:2895`「退款单必须与原退货单据勾稽」至今无人处置**——
   其下 `:2899` 逐字「提交时校验……**不允许静默通过**」，是一条**无条件提交闸**。
2. **`P-21` 指向一个规格里不存在的概念。** 实测规格全文 `冲正` 命中 **0**；
   该概念本应由 `S-08` 引入，**而 `S-08` 第三轮被驳至今未落**——
   **`P-21` 的前置是 `S-08`，本工单把两者列为平行条目。**
3. **`S-09` 推翻的规格原文不止一处。** 规格 `:297` 与 `:880` 各有「回滚剩余可开比例」表述，
   本工单未列；第四批起草方按「只允许一次」一个关键词排查，恰好漏掉。
4. **`P-15` 与第一批已落的 `PRD:2703` 无条件对撞。** 该句逐字「红字冲销或作废登记**不修改**原应收明细条目」，
   而 B-6 的释放核销就在红冲登记的同一事务内修改它。**三轮四次起草都栽在这一句**，
   而本工单未授权任何条目改写它。

**结论：本工单低估了 B 簇的耦合度**——把一个需要**一次性整体改写**的语义变更，
拆成约 20 条各自起草的条目。**建议改为一次交付**，由一个人在完整上下文里
一口气改完规格与 PRD 的全部相关段落，`00h` 的四个堵点即该次改写的必答清单。

**第四批有 4 条通过对抗核但未落**（`S-04`／`S-05`／`S-06` 规格:311 三栏、`P-37-6.5.3`），
文本可供那次整体改写取用；本卷不单独落，因 `S-07`／`S-07b` 被驳、B-6 规格侧仍不齐。

### 本批核出的三处工单自身错误

1. **附录乙行号整体偏小约 15 行**。2026-08-20 的穷尽核查已回写过 9 行状态列，行号随之下移。
   受影响的 `P-06` `P-25` `P-35` `P-42` `P-43` 按工单行号取串会全部落空。
   **且这几条的实质结果已达成**——状态列现值已是「已关闭，见…」，工单假设的「待决」已失效。
2. **`P-33` 的「四处」是语义计数不是逐字计数**。实测「只允许冲回一次」逐字仅 2 处（`:2393`、`:2464`）；
   `:2411` 逐字为「各自只允许一次」、`:2441` 逐字为「只允许一次」。
3. **`P-47` 的三处行号已漂移**：`4526`／`4533`／`4545` 实为 `4541`／`4548`／`4560`。



> **边界**：本仓既定纪律是裁定「**请求**规格修订」而非径自修改。
> F-18 结论八逐字「**规格是要求方，不是承接方**；本裁定不删要求，只登记本部署不交付」；
> F-08 那次改规格逐字写「**本轮由使用方授权改规格**」。
> 故**本工单只列锚点与改法，不代为执行**，规格侧尤其如此。
>
> **一处对上轮警告的收窄**：上轮曾警告「F-10 有 4 条已撤下，机械回写会把撤下的写进去」。
> 实测该风险**对这 27 条不成立**——撤下的 4 条是 `A-6b`、`D-12-1` 一支、`D-12-4`、`D-13-1`，
> **它们本来就不在最终 27 条之列**（27 条含 `A-6a`，不含 `A-6b`）。
> 真风险只剩一个：**39 条「必改」与 38 条「应改」修正过原裁定**，照原文回写会写进旧版本。

# 以下为 2026-08-20 当时的原工单正文（历史快照）

## F-10 回写工单（27 条裁定 → 规格与 PRD）

**历史原状态：当时待授权后执行。该状态已被本文开头的 F-48/F-50 supersession 台账取代，不是现行待办或开工阻断。**
编制日期 2026-08-20；依据 `docs/superpowers/specs/2026-08-17-f10-ruling-detail.md`（613 行，下称「详本」）。

---

## 0. 边界：本工单为什么是「请求」而不是「执行」

本仓的既有纪律是裁定**请求**规格修订，不得径自改写规格：

- `00c-gap-ruling.md:3696` 逐字：**「规格是要求方,不是承接方」**；同段逐字「本裁定不删要求,只登记本部署不交付」（F-18 结论八「规格不删句」，行 3693 逐字为该小节标题）。
- 例外须由使用方逐轮授权：`00c-gap-ruling.md:1950` 逐字「**裁定方向：规格让步（本轮由使用方授权改规格）**，计划与代码随之重取承载物；四个机制里有三条机制保证在本平台不再成立，一律删除而不换等价物。」——注意该授权逐字系于 **F-08 那一轮**，不自动延伸到 F-10。

因此：

1. **规格侧（25 处）一律待使用方逐条授权后方可动笔**，且授权应写成与 `00c:1950` 同形态的一句留证。
2. PRD 侧（62 处）虽为第二层权威，但其中 30 余处直接改写附录乙与第 6.16 节的「待决」登记，属对外承诺面，同样建议一并授权。
3. 本工单内凡标注「**非裁定要求·核查新增待裁项**」者，是核查中发现的裁定未覆盖冲突，**工单不代裁**，须使用方另行表态后才进入执行队列。

**本轮核实口径**：所有规格与 PRD 锚点的行号与现原文，均在本轮以 `sed -n 'Np'` 逐行实取核过（规格 2000 行、PRD 4660 行）；计划侧行号只抽核了 `计划06:612`、`计划06:913` 两处，其余计划侧引用**沿用详本原文，未在本轮复算**——入卷时须当场重验（详本自陈计数失配已栽过三次）。

---

## 1. 总表（27 条）

「修正」列指详本自查缺陷表（39 必改 + 38 应改 + 4 登记即可）对该条的修正条数；**凡非零者，回写必须按详本正文的修正后版本，不得按裁定草案原文**。

| 编号 | 标题（略） | 撤下 | 被必改/应改修正 | 回写已做 | 改规格 | 改 PRD |
|---|---|---|---|---|---|---|
| A-1 | 影响面处置台账 | 否 | 5 条（撤 WAIVED、加 replay、目录算定、DONE 分支 CHECK、item_total 断言） | 未做 | 2 | 1 |
| A-2 | 下游影响关系取静态声明 | 否 | 3 条（archcheck 换判据、覆盖面写死七类、目录非注册数） | 未做（本就不动规格/PRD） | 0 | 0 |
| A-3 | 三条驱动杠杆 | 否 | 2 条应改（SLA 定时器、实例状态不阻断） | 未做 | 0 | 2（其一为依赖项，改法归 A-5） |
| A-4 | 「作废」≠「终止」 | 否 | 2 条应改（管辖面限合同对象、判据二降评审） | 未做 | 0 | 1 |
| A-5 | TERMINATING 态与四条边 | 否 | **0 条**（81 条缺陷表内零命中） | 未做 | 2 | 4 |
| A-6a | 新增 TERMINATION chain_kind | 否（撤下的是 A-6b） | 本身即修正产物（原 A-6 被 3 条必改打掉） | 未做 | 0 | 0 |
| A-7 | 七条影响面规则 | 否 | 6 条 | 未做 | 0 | 2 |
| A-8 | 收款计划期次加 status/void_reason | 否 | 1 条应改（档位上调计划级新增） | 未做 | 0 | 0（另有 1 处提请拍板） |
| A-9 | 两个新事件与「十个」读法更正 | 否 | 2 条（18 还是 20 降为待数；计划07:487 触发方同改） | 部分（仅 00c:2789-2791 的读法更正已落） | 0 | 0 |
| A-10 | 三条 E2E 加一条反向用例 | 否 | 1 条应改（WAIVED/approval_ref → decision_reason） | 未做 | 0 | 0 |
| A-11 | 跨阶段接线次序 | 否 | 2 条必改（逐阶段注册数 3/4/6/7；占位项） | 未做 | 0 | 0 |
| A-12 | 前置阻断扩面与四类例外 | 否 | 1 条登记即可（九条→十条断言） | 未做 | 0 | 3 |
| B-1 | U-H-07 更正凭证入口：开 | 否 | 6 条（含撤 OPENING_BALANCE_LINE 来源、档位上调规格级） | 未做 | 1 | 4（含 1 处工单自提） |
| B-2 | U-H-08 手工凭证入口：不开 | 否 | 2 条应改（(c) 降为陈述、错引更正） | 未做 | 2（含 1 处建议） | 3（含 1 处建议） |
| B-3 | U-D-02 资金单据冲正 | 否 | 2 条应改（档位上调规格级、错引更正） | 未做 | 2 | 6 |
| B-4 | U-D-09 允许部分金额红冲 | 否 | 4 条 | 未做 | 1 | 10 |
| B-5 | 硬伤一：台账改追加式 | 否 | 2 条（source_type 加 INVOICE_REVERSAL；索引节号） | 未做 | 0 | 3 |
| B-6 | 硬伤二：红冲释放核销转预收 | 否 | 2 条必改（三处封闭 CHECK；改借贷两栏不是附加规则栏） | 未做 | 4 | 7 |
| B-7 | 硬伤三：勾稽视图改按期间累计 | 否 | 2 条（逐视图给式；索引节号） | 未做（裁定本不要求动规格/PRD） | 1★ | 1★ |
| B-8 | 销项发票行明细模型 | 否 | 3 条（重写判定式、更正「空实现」描述、阶段 10 落点） | 未做 | 0 | 4（含 2★） |
| C-1 | 补偿动作可配面 | 否 | 4 条（撤 SHA-256 冻结、撤配置项迁入、撤第 9 个 suite） | 未做 | 2 | 4 |
| C-2 | 可配的是阈值不是迁移 | 否 | 5 条（撤 archcheck 规则、改判「7 天作废」为阈值） | 未做 | 2 | 3 |
| C-3 | U-A-08 默认审批链与无链即拒 | 否 | 5 条（作用面收窄、剔到款登记、角色码大写、撤 fail-open） | 未做 | 0 | 2（其一为核对动作） |
| C-4 | 能力闸与重新认证次序 | 否 | 3 条必改（撤整段路由豁免等） | 不适用（动到不含规格/PRD） | 0 | 0 |
| D-1 | 备份保留期 D 与治理 | 否 | 6 条 + 2 支撤下 | **部分已做**（规格:1206 取值一半，commit b7f2687） | 3 | 0 |
| D-2 | 保留窗口自检项与降级窗口 | 否 | 3 条（severity 改 Informational、五项 basis、同批清单先修） | 未做 | 1★ | 0 |
| D-3 | 对外表述权限与诚实披露 | 否 | 4 条（补 PRD 连带、撤独立 CI 检查、行号更正） | 未做 | 2 | 2 |

★ = 非裁定要求，核查新增的待裁登记项，须使用方先表态。

**合计：规格 25 处（其中 2 处为待裁登记）、PRD 62 处（其中 3 处为待裁登记、2 处为工单自提建议）。**

---

## 2. 撤下的条目：不得回写

详本第三节标题逐字「**## 三、撤下的 22 条（各因反方哪一条必改）**」（详本:419）。与规格／PRD 回写直接相关的撤下项如下，**任何一条出现在回写文本里都视为把已被否决的旧版本写进了权威文档**：

| 撤下项 | 详本逐字依据（节选） | 回写禁令 |
|---|---|---|
| **A-6b** 终止列为第七类高风险操作（含重新认证） | 「A-6b ……——撤下，进 still_open。两条必改：其一代价被压低到不可接受，「六类」口径在 specs/ 下逐字命中 36 处」 | 规格:1050 逐字「高风险操作为合同生效、付款、开票、财务过账、期末结账和敏感数据导出六类」**一字不动**；A-6a 明写「规格与 PRD 一字不动」 |
| **A-1 的 WAIVED 状态与 approval_ref** | 「A-1 的 WAIVED 状态与其 approval_ref——撤下，不进本轮……落地后只能靠测试代码手工塞一个 UUID」 | PRD 3.5.5 只写「以 state=DONE 加非空 decision_reason 表达」；A-10 反向用例一律用 `decision_reason` |
| **A-2/A-7 覆盖面含规格第 8 章第 13 步** | 「——从覆盖面显式摘出……该「缺一不通过」的评审判据按定义恒假」 | 规格:874 追加句必须逐字含「第 13 步的已过账凭证不列为处置项」 |
| **B-1 的 OPENING_BALANCE_LINE 来源** | 「B-1 更正凭证的期初余额批次来源（source_ref_kind = OPENING_BALANCE_LINE）——撤下」 | 规格第 5.2 章新增条**不得**出现「引用已确认期初余额批次行」 |
| **B-1 原守卫二/三（行级引用+行级累计）** | 「——撤下，换为凭证级引用与凭证借方合计上限。两条互相打死」 | 一律写「每张更正凭证必须且只能引用**一张**已过账凭证」 |
| **B-1 原守卫五（重跑全局十项勾稽）** | 「——撤下，换为「只重跑本次分录触及的勾稽项且判据为不劣化」」 | 不得写「任一项差额非零整笔拒绝」 |
| **C-1 的 SHA-256 冻结机制** | 「C-1 的 SHA-256 冻结机制（内置快照为运行期权威、不一致即拒绝一切写入并持续告警）——撤下」 | 规格:926 后新增条**一个字都不得**出现「快照哈希不一致即拒绝写入」 |
| **C-1 迁入两个 EP__FINANCE__ 配置项** | 「——撤下。迁入目标不存在（九列里没有任何表达「本动作是否需要审批」的布尔列……）」 | 不得从配置文件删除这两项 |
| **C-1 拟增 suite `COMPENSATION_POLICY`** | 「——撤下且不得恢复。F-21/F-52 后现行九套的第九套固定为 `RULE_SEMANTICS`，`COMPENSATION_POLICY` 不成为第十套」 | 不得写「新增 `COMPENSATION_POLICY` 自动测试套件」 |
| **C-2 的 archcheck 规则 configurable-transition-disjoint** | 「——撤下……构建期读不到数据库」 | 规则名不得出现在任何回写文本 |
| **C-2「开票 7 天内允许作废」作为可配迁移** | 「——改判为阈值参数可配、迁移本身冻结」 | PRD:4051 追加句必须写「ISSUED → VOIDED 这条迁移本身不可配」 |
| **C-3 的 fail-open 挂 DegradationLedger** | 「——撤下，统一为 fail-closed」 | U-A-08 关闭说明须逐字写「fail-closed，不开降级窗口」 |
| **C-4 整段豁免 /api/v1/platform/high-risk-requests\*** | 「——撤下。规格:577 逐字把审批列在移动端「转桌面端完成」之列」 | 规格:577 不加任何豁免例外（实测该行原文未动，方向已一致） |
| **D-12-1 归档段销毁一支、第三段（丁）** | 「——撤下，进 still_open」／「——撤下。与其第五段守卫（5）……自相打架」 | D-1 规格侧不得写归档段销毁 |
| **D-12-2 的 ck_offsite_sinks_capacity 硬 CHECK** | 「——撤下，改为可记录加告警加降级窗口」 | 不得写「容量不足即拒收」 |
| **D-12-4 的 A.6 追加演练、演练门禁断言** | 「——撤下，进 still_open」／「——撤下……判据不可复算不可复现」 | 附录 A.6 不加第三次演练 |
| **D-13-1 六项任务效率验收** | 「——整条撤下重裁，进 still_open」 | 第 22 章不加第 16 条、不加 RG-TASK-ECONOMY |
| **D-13-3 独立 CI 文本检查** | 「——撤下，并入计划14:568 既有的十项禁用措辞清单」 | D-3 不得新增独立 CI 检查；规格第 21.22 节须**显式排除**在文本检查对象之外 |

另一处必须同批清理的**卷内对撞**：`00c-gap-ruling.md` 第 2792 行一带（F-10 摘要第三节）逐字仍写「批次加逐项、双唯一约束、八档退避、死信、`WAIVED` 加 `approval_ref`」——与详本已撤下 WAIVED 正面冲突（实测 00c 内 `WAIVED` 命中 1 处）。按权威顺序详本是 F-10 正文、00c 只是摘要，回写时须同批改掉该句，否则下一轮复核会照 00c 把 WAIVED 再造一次。

---

## 3. 工单正文（按详本第五节「落地次序」编排）

详本:499 逐字「落地次序，按依赖分五股。」并给出一条前提纠正（详本:497）：「**影响面机制只挂合同终止一个上游事件**……发票红冲的下游处置走 B 簇同事务路径，与影响面机制无前后依赖。两条路各自成链。」

### 3.0 必须合并改写的锚点（先看这张表，再看批次）

同一行被多条裁定动到，**分次改会互相覆盖**：

| 锚点 | 被哪几条动到 | 合并要求 |
|---|---|---|
| 规格:288（5.2 章 CLM 条目） | A-1、A-5 | 一次改写，追加终止语义 + 闭合要求 |
| 规格:874（第 8 章第 3 步末） | A-1、A-5 | 一次追加；形态服从 A-5「不新增步骤」的更严口径 |
| 规格:311（事件-分录表「红字冲销与作废」行） | **B-3、B-4、B-6** | 三条同批：B-6 改借方栏与贷方栏，B-4 改「只允许冲回一次」，B-3+B-6 各加一句附加规则 |
| 规格:926（9.1 章清单末条之后） | C-1、C-2 | 合并成一段书写 |
| 规格:359-361（总账块） | B-1、B-2 | B-1 条在前、B-2 条在后，一次插入 |
| PRD 3.5.5（新增节，插在 :1057 与 :1058 之间） | **A-1、A-3、A-7、A-12** | 四条合并为**一次新增**，不得各建一节 |
| PRD:2977（凭证的产生方式） | B-1、B-2 | 同一句的两半，一次改完 |
| PRD:4051 / :4052（10.4.4 能改/不能改） | C-1、C-2 | 各自合并追加 |
| PRD:2431（6.5.3 应收台账句） | B-3、B-5 | 同一段，一次写 |
| PRD:1074 / :1076（3.6 状态机） | A-3（依赖）、A-5（改法） | 按 A-5 执行 |

---

### 第一股 · 阶段 3 前置（A-1 / A-2 / A-9）

详本:501 逐字「A-1 未落地之前，A-3、A-5、A-7、A-10、A-11、A-12 一条都做不了。」
A-2 与 A-9 **在规格与 PRD 上零落点**（A-9 动到逐字「规格与 PRD 均不动。」详本:244），其落点在 `docs/impact-catalog.md`、`docs/event-catalog.md`、00b 与阶段计划，本工单只登记不展开。

**规格改动 S-01（A-1 + A-5 合并）**
- 锚点：规格:288
- 现原文（起首逐字）：「- CLM：多类型合同、模板、条款、修订、电子签章、实体印章、履约、义务、收付款和档案。」末句逐字：「支持合同到期提醒：按合同有效期、交付节点日期和收付款计划到期日生成提醒，提醒规则与提醒渠道由第 5.1 章低代码定时器与通知承载，不建独立提醒引擎。」（本轮实测该 bullet 全文无「终止」二字）
- 改成：在末句后追加——「支持已生效合同的终止：终止是已生效合同的唯一退出动作，与生效前废弃（作废）不是同一件事。终止须经审批，合同先进入终止处置中状态，其已派生的订单、采购需求、项目任务、收款计划期次、交付节点与已发生的交付确认、销项发票逐项登记处置并推至闭合；台账全部处置项完成且无失败项时合同方进入已终止，处置项未闭合前合同不得进入终态。已发生的事实一律不回退，但必须列出、追踪并推至有结论。台账的落库对象、处置类别、生命周期与闭合判定见 PRD 第 3.5.5 节。」

**规格改动 S-02（A-1 + A-5 合并）**
- 锚点：规格:874 行末（不新增第 15 步）
- 现原文末句逐字：「派生失败按第 15.2 章进入死信与人工修复并写入审计。」
- 改成：追加一句——「已生效合同的终止是本闭环的例外出口，不构成闭环步骤：终止发起后，本步派生的销售订单、采购需求、项目任务、收款计划、交付节点五类对象，以及第 7 步发票开具登记与第 8 步交付确认两类已发生事实，合计七类，按影响面处置台账逐项处置，全部闭合后合同方到达已终止；**第 13 步的已过账凭证不列为处置项**，其更正手段仍按第 5.2 章走红字冲销与更正凭证；未闭合期间的下游前置阻断与其例外见 PRD 第 3.5.5 节。」
- 纪律：「合计七类」与「第 13 步显式排除」是被修正条款的直接产物，措辞不得回退为笼统的「各类已发生事实」。

**PRD 改动 P-01（A-1 + A-3 + A-7 + A-12 四合一，新增节）**
- 锚点：PRD:1057（空行）与 PRD:1058「### 3.6 合同状态机」之间。本轮实测 PRD 全文 `3.5.5` 零命中，**该节不存在，属新增**。
- 标题逐字：`#### 3.5.5 影响面处置台账`
- 内容五块，一次写成：
  1. **台账形状与闭合（A-1）**：批次 + 处置项两层；批次记来源模块／来源单据及版本／来源事件／终止原因／批次状态／项总数／已完成数，同一单据同一版本同一事件只得一份批次；处置项记规则码／目标模块与单据／处置类别（自动关闭、自动取消、人工决策、仅告知）／项状态／重试次数／决策人／决策时间／决策理由。**不设「豁免」出口**，人工决策项的「明确不处置」以状态置已完成加非空决策理由表达，理由为空不得置为已完成。**项集按固定目录算定，首版恒为七类，与各模块是否已接线无关；未接线类别照常产出占位项、保持待处理、不计入已完成数。** 闭合判定唯一：已完成数等于项总数且无失败项 → 批次已完成、合同可进已终止；有失败项 → 批次失败、合同保持终止处置中并显示待人工修复。**失败项有整批重放出口，重放自动去重、不产生重复单据。**
  2. **三条驱动杠杆（A-3）**：甲状态阻断（合同停在终止处置中，重复发起终止不改变状态）；乙待办驱动（每个人工决策项生成一条人工任务，承载流程定义 `clm.contract_termination_disposition`，**必须为每个人工决策节点登记一条 SLA 定时器**，超时时长由 `EP__IMPACT__MANUAL_ITEM__SLA_DAYS`（出厂默认 5 天）控制，到点只写超时标记并产生「流程时限提醒」通知、不推进实例；**处置项推进由平台影响面组件驱动、不依赖流程实例状态**，实例进入人工干预既不阻断处置也不阻断合同离开终止处置中）；丙前置阻断（见第 4 块）。另须写明：**处置未闭合不作为期末关账的拦截项**。该流程定义的 `max_instance_duration_days` 本轮未定，**不要在 PRD 里填数字**。
  3. **七条规则表（A-7）**：列为 规则码｜对象｜取数范围｜处置方式｜承接模块与阶段，七行——销售订单行（OPEN/PARTIALLY_DELIVERED；零交付取消、有交付关闭并写「合同终止 <合同编号>」；**同批取消该订单行底下 status=PENDING 的分批交付行**）、交付节点（PLANNED/ACTIVE 自动取消）、交付确认（人工决策）、收款计划期次（未被开票占用者自动置作废并写原因，是否已开票经只读端口判定）、采购需求（**三支来源键**：CONTRACT: 前缀、属本合同的 SALES_ORDER:、source_contract_id 等于本合同的 PROJECT_TASK:；PENDING/PARTIALLY_ORDERED 自动关闭，ORDERED 人工决策）、项目任务（NOT_STARTED 自动取消，IN_PROGRESS 人工决策）、销项发票（人工决策，在作废与红冲两条既有路径里选一或明确不冲）。表后三句：目录恰七条不设第八条凭证项及其理由；**四条**会产出人工决策项的分支（第 3、7 恒为该类，第 5 条 ORDERED 与第 6 条 IN_PROGRESS 同样产出）；「已发生的一律不回退」不等于不列出、不追踪、不推。
  4. **五项阻断 + 四类例外（A-12）**：阻断清单**穷举五项**（新开销项发票申请提交、新登记交付确认、订单变更、按该合同新建采购需求、合同修订 amend），在 TERMINATING 或 TERMINATED 下一律阻断；例外清单**必须写死四类**（销售退货登记、发票作废与红冲登记、客户退款登记、采购退货登记），在两态下一律不得被阻断，理由须写入：这四类是人工决策项唯一的完成手段，加通用守卫会使闭合判定永远不可满足。核对纪律：第一张清单是穷举不是举例，日后加第六项须另裁并核对是否会挡住某个人工决策项的完成手段。
  5. **口径提示**：表名、列名、CHECK 取值与 crate 名落阶段 3 计划，PRD 侧只写业务口径。
- 依赖：本节引用的「终止处置中」由 A-5 写进 3.6 表，**两批不同步则本节引空**。

---

### 第二股 · 阶段 6（A-4 → A-5 → A-6a → A-8 → A-7 → A-12 → A-10）

详本:503 逐字「其中 A-4 必须在 A-5 之前——不先钉死词义，状态机命名会被反复重新解释」。

**PRD 改动 P-02（A-4，新增段）**
- 锚点：PRD:1077（表末行，实测逐字「| 已作废 | 生效前废弃 | 终态 | 不产生任何派生单据 | 无 |」）与 PRD:1079 之间（:1078 为空行）
- 改成：新增一段词义说明——「在合同这一类对象上，「作废」只指生效前废弃，入边只有草稿到已作废与已驳回到已作废两条，守卫为该合同无任何派生记录。已生效合同的退出动作一律叫「终止」，其状态为终止处置中与已终止，两者不是一件事；界面文案、错误提示、交付说明与对外表述一律按此口径，合同错误码中不得新增任何含作废语义且作用于已生效及其后状态的码。**本段的管辖面只限合同对象**：销项发票的作废（只对已开具的发票成立）、销售订单与收款计划期次的作废各按其自身状态机，均不属本段所称的合同作废。使用方口头所说的「合同作废」，在本版内一律按「终止」受理。」
- 纪律：「管辖面只限合同对象」是被应改条款强制保留的修正，**删掉它下一轮会被拿来反推「发票作废写错了」**。PRD:1063、:1068、:1077 三行本轮实测与裁定一致，**不改**。

**PRD 改动 P-03 ~ P-06（A-5，状态机）**
- P-03｜锚点 PRD:1074，现原文逐字「| 履约中 | 同上 | 已终止 | 提前终止，条件见开放问题 | 待定 |」→ 改为目标状态「终止处置中」、触发者「合同责任人」、触发条件「TERMINATION 审批链结论通过、审批人不等于发起人、终止原因非空、乐观锁版本匹配」。
- P-04｜锚点 PRD:1076，现原文逐字「| 已终止 | 提前终止 | 终态 | 已派生单据的处置见开放问题 | 无 |」→ 「已派生单据的处置见开放问题」改为「已派生单据与已发生事实的逐类处置按 3.5.5」，终态不变。
- P-05｜在 :1074 之后、:1075 之前**新增三行**：已生效 → 终止处置中（提前终止且派生失败，其余守卫同履约中一行）；终止处置中 → 已终止（批次已完成且已完成数=项总数，触发者系统）；终止处置中 → 终止处置中（存在失败项，批次置失败、显示待人工修复）。并在状态含义处写明「终止处置中」为**非终态**。
  **算术含混须使用方拍板**：A-5 动到逐字「新增四行、改 1074 与 1076 两行」，但四条边里「履约中→终止处置中」恰是 :1074 改写后那一行；若照字面另起一行，表内会同时留下「履约中→已终止」与「履约中→终止处置中」两条互斥边。**本工单按三行新增 + 两行改写落地。**
- P-06｜锚点 PRD:4531（附录乙 U-E-12，实测状态列仍「待决」）→ 状态列改为「**已关闭，见裁定 F-10 A-5（状态机）、A-6a（审批链）与 A-7（派生对象处置）**」。
  **不得照抄 A-5 逐字的「已裁定」**：PRD:4429 实测逐字「- 状态列只有两种取值：待决与已关闭。已关闭的事项保留在表内，注明关闭方式。」，现存两条已关闭项 PRD:4447／:4448 即体例。若使用方坚持「已裁定」，须同批改 4429，二者必居其一。**该纪律适用于本工单全部附录乙改动（U-D-02、U-D-09、U-H-07、U-H-08、U-A-08、U-E-12）。**

**A-6a**：动到逐字「规格与 PRD 一字不动，这是本条降级后的全部代价面」。**本股在规格/PRD 侧无工单**，其落点为计划06:193 的 `chain_kind` CHECK 六值增七值、计划06:524 端点表传播全局唯一码 `PLATFORM.SOD.SELF_APPROVAL_FORBIDDEN`（按 C-24 不计入阶段 6 条数，须在计划中写明否则会被误判为漏登记）、阶段 4 审批链场景清单。

**A-8**：动到不含规格与 PRD。**但有一处提请拍板**：A-8 第四条要求提醒取数排除已作废期次与 TERMINATING/TERMINATED 合同的全部期次与交付节点，而 PRD 第 3.9.2 节（PRD:1125-1135）与 PRD:4100 逐字仍承诺按**全部**期次与交付节点发提醒，无任何排除口径。不同批加，PRD 层将与阶段 6 视图新语义不一致。**是否纳入回写，请使用方裁。**

**PRD 改动 P-07（A-7）**
- 锚点：PRD:4624（U-J-13），实测逐字「| U-J-13 | 合同变更或续签导致已派生任务不再需要时的处置策略：保留、作废还是新增补充任务（第 9 节） | …… | 产品负责人 | 待决 |」
- 改成：**状态列保持「待决」不变**，仅在描述列末尾加限定语——「本事项只适用于合同变更或续签导致的派生计划变动；合同终止场景按裁定 F-10 A-7 的 `CLM_TERM_PROJECT_TASK` 处置，不适用本事项的任何临时取值。」
- 理由：不加这句，U-J-13 与 A-7 第 6 条正面冲突（计划12:887 临时取值逐字「保留既有任务，不自动作废」，与 A-7 对 NOT_STARTED 任务的自动取消互斥）。同句须同批加到 计划12:887。

**PRD 改动 P-08 / P-09（A-12，谓词扩面）**
- P-08｜锚点 PRD:2289，现原文逐字「- 合同状态校验：合同未生效、已终止的不允许提交发票申请。」→ 改为「- 合同状态校验：合同未生效、终止处置中或已终止的不允许提交发票申请。此处「终止处置中或已终止」对应合同状态 TERMINATING 与 TERMINATED 两态，谓词口径与 3.5.5 一致；本条不适用于 3.5.5 所列的四类反向补偿动作。」
- P-09｜锚点 PRD:2324，现原文逐字「| 合同未生效或已终止 | 阻止提交，定位到合同字段 |」→ 左栏改为「合同未生效、终止处置中或已终止」，**右栏一字不动**。
- 穷举佐证（本轮实测）：PRD 内「已终止」共 8 处——:1074／:1076 属状态机（A-5 落点）；:1431、:1603、:1669、:1670、:1671、:1676 六处全是**供应商准入状态**，与合同终止无关。故按合同已终止判定的下游阻断**恰为 :2289 与 :2324 两处，未多未漏**。

**A-10**：动到全在阶段 6／12 计划（第 8.3 小节测试清单、第 8.4 小节 E2E 表接 E2E-6-07 之后新增两行）。规格与 PRD 无落点。**回写反向用例一律用 `decision_reason`，不得出现 `approval_ref`。**

**A-11**：动到全在阶段 3/6/7/10/12 计划。跨股硬约束（详本:509）：**逐阶段注册数期望表 3 / 4 / 6 / 7 必须与四个阶段的退出条件同批写入，晚一个阶段写，那个阶段的门禁就是空的。**

---

### 第三股 · B 簇财务补偿（**内部次序不可颠倒**）

详本:505 逐字：「第三股（B 簇财务补偿，内部次序不可颠倒）：**B-5 → B-7 → B-6 → B-4 与 B-8 必须同批 → B-3 与 B-6 同批入卷 → B-1 落阶段 9b → B-2 与 B-1 同批**。」

#### 批次 3-A：B-5（台账改追加式）

- **P-10｜PRD:2624 之后新增两条**（6.9.1 台账构成）。现原文 :2624 逐字「- 应收明细条目在销项发票开具登记时产生，其金额口径为规格第 5.2 章财务规则条目的开票事件在应收账款科目上的入账金额。」
  新增其一：「- 红字冲销或作废登记不修改原应收明细条目，而是追加一条冲销类明细条目：其条目类别为冲销、指向被冲销的原条目，原单据金额取本次红字发票价税合计，已核销金额为零，未核销余额等于原单据金额，到期日取原条目，原始业务日期取冲销登记日期，会计期间取红字凭证的期间解析结果。」
  新增其二：「- 台账对外的有效未核销余额口径为：开票类条目的未核销余额，减去指向它的全部冲销类条目的未核销余额之和；该口径不为负。冲销类条目不单独进账龄。」
  **PRD:2626 的「台账中不允许出现负数未核销余额」一句不改**（追加式下每一行都为正，负号只出现在聚合口径里）。应付侧 PRD:2658 按引用自动承接，**不另写**。
- **P-11｜PRD:2639**，现原文逐字「- 账龄的计算基数是该条目的未核销余额。」→ 改为「- 账龄的计算基数是该条目的有效未核销余额，即该开票类条目的未核销余额减去指向它的全部冲销类条目的未核销余额之和；冲销类条目本身不单独进账龄。」应付侧 PRD:2671 按引用承接。
  **连带**：阶段 10 第 4.3 节核销分配第 3 步的上限口径必须同批改，漏一处即出现「按 open_amount 核销、按 effective_open 记账龄」的不一致，**这种不一致不会报错只会算错**。
- **P-12｜PRD:2431**（与 B-3 合写），现原文逐字「- 应收台账中由原发票产生的应收明细条目按本次冲销结果调整未核销余额，见 6.9。」→ 改为「- 应收台账不修改由原发票产生的应收明细条目，而是按 6.9.1 追加一条冲销类明细条目，原条目的未核销余额一字不动，对外的有效未核销余额按 6.9.1 的差额口径下降；账龄按 6.9.3。」
  「调整未核销余额」六字正是被判死的原地改写口径，**只改指向不改动词等于把已否决的写法留在 PRD 里**。

#### 批次 3-B：B-7（勾稽视图改按期间累计）

裁定动到逐字「规格第 10.2 章不改，PRD 第 6.13.2 不改」。**以下两项是核查新增的待裁登记，不是裁定要求**：

- **S-03★｜规格:1352**（第 17.3 章「子账与总账勾稽」），本轮实测该行为规格内「未核销余额合计」的唯一命中，逐字仍含「应收台账未核销余额合计等于应收账款科目余额」——正是 B-7 判死的**当前值**口径。不改则规格判定文本与阶段 10 取数式相反，且 B-7 的属性测试五没有规格依据。建议改为按法人与会计期间累计的口径。**工单不代裁。**
- **P-13★｜PRD:2787-2790**（6.13.1 勾稽项表四行），实测逐字仍为「未核销余额合计」「余额合计」。同上理由建议改为期间累计表述。
  **一并登记以免误改**：PRD:2816-2817「原单据金额等于已核销金额加未核销余额」在追加式下逐行仍然成立，**无须改动**。

#### 批次 3-C：B-6（红冲释放核销转预收）+ B-3（资金单据冲正）同批

- **S-04｜规格:311 借方栏** → 销项加「应收账款，金额为本次释放的已核销金额」；进项加「预付账款，金额为本次释放的已核销金额」。
- **S-05｜规格:311 贷方栏** → 销项加「预收账款，金额为本次释放的已核销金额」；进项加「应付账款，金额为本次释放的已核销金额」。
  释放额一律为 min(该条目已核销金额, 本次红字发票价税合计)；金额为零时不生成分录行，不新增凭证来源类型取值。**必须与借方栏同批改——只改一栏当场借贷不平。**
  **强制理由（被必改点名）**：只在附加规则栏加一句**不能**给事件-分录映射新行提供依据，风险四那条核对与断言当场不成立。本轮实测规格:311 全行内「预收账款／预付账款」**零命中**，与该判断一致。
- **S-06｜规格:311 附加规则栏** → 栏末追加「；原发票的应收或应付明细条目已被核销的，在同一事务内先按 min(…) 释放核销再登记冲销，释放额按销项转预收账款、按进项转预付账款，资金腿零变动；原核销来自客户退款的一律拒绝登记，须先冲正该退款单；后续重开新票时该预收按到款事件的自动核销条款核销」。**从属说明，不能替代借贷两栏。**
- **S-07｜规格:310（退款行附加规则栏）** → 删去死分支「原款项已核销至应收或应付且退货部分已完成红字冲销的冲减应收账款或应付账款」，改为「原款项仍挂预收或预付的，以及原核销已由红字冲销释放转入预收或预付的，一律冲减预收账款或预付账款」；并把「退款单据必须与原退货单据勾稽」改为条件式。本轮实测该行逐字仍含被删分支。
- **S-08｜规格:315-318 与 :320 之间新增「资金单据冲正：」规则块（B-3）**。本轮实测规格全文「冲正」**零命中**，属新增。内容五条：唯一更正路径为冲正单、按原凭证逐行取反；冲正原因限五项不设第六项；一张资金单据只能被冲正一次且冲正单不可再冲正；属高风险操作须重新认证并审批；**禁用场景**——销项发票红冲时原到款已核销的一律走「已核销款项转预收」，不得以资金单据冲正解此场景（理由：逐行取反必然把银行存款腿贷回，而款项并未退还客户，资金账当场与银行对账单不符）。
- **PRD 侧（B-6）**：P-14 PRD:2431 之后新增四路分支段（人工核销→新增预收条目；预收自动核销→回增原条目余额；客户退款→拒绝并提示先冲正；此前冲销释放→不参与），并在 6.5.5 异常表新增一行；P-15 PRD:2634 之后新增反向核销关系记录条（不删不改原记录，追加指向它的反向记录，成对相消）；P-16 PRD:2684 与 :2693 各改为两个产生方式；P-17 PRD:2706 末段改写（不改则「挂账只能由到款登记与付款登记的剩余款项产生」这句逐字把两条新路径封死）；P-18/P-19/P-20 PRD:2726、:2738、:2773 **三处必填改条件必填，必须同批，漏一处即在校验层或提示层仍然堵死**。
- **PRD 侧（B-3）**：P-21 PRD:2533 由「更正路径规格未定义」改为冲正单已决表述；P-22 PRD:2603 与 P-23 PRD:2759 指向 6.7.6 自动随动、建议在 6.8.7 表后补一句付款方向口径；P-24 PRD:2890（第 6.16 节 F-14 行）；P-25 PRD:4498（U-D-02）状态列改「已关闭，见第 6.7.6 节」。
  **6.16 节体例须先定**：实测表头 PRD:2875 逐字只有「| 编号 | 待决策项 | 引用位置 |」三列、**无状态列**，节前言 PRD:2873 逐字「本节不自行取值，统一由后续决策补齐」。关闭方式只能二选一——删除该行，或在待决策项列末尾追加「（已由裁定 F-10 B-3 关闭，见 6.7.6）」。**本节还有 F-08 等条目将被别的裁定关闭，体例应一次定好。**

#### 批次 3-D：B-4（部分红冲）+ B-8（发票行明细）**必须同批**

- **S-09｜规格:311 附加规则栏**（与 S-06 合并书写）：「同一张发票的作废与红字冲销互斥，只允许其一，且只允许冲回一次」改为「同一张发票的作废与红字冲销互斥；作废只允许全额且只允许一次，且只有尚未发生任何红字冲销的发票可登记作废；红字冲销允许分次部分金额登记，各次累计不含税金额、税额与价税合计分别不得超过原发票对应金额，累计冲满后原发票进入已全额红冲终态」。其余三段**一字不动**——最后一段（价格调整红字发票按进项方向登记）正是本条的立论依据。
- PRD 侧十处：P-26 PRD:2388-2391 状态机改四态（新增「部分红冲」中间态与两条自环／转终态边）；P-27 PRD:2393；P-28 PRD:2417（实测逐字「| 原发票 | 是 | 只能选择状态为已开具的销项发票 |」——**不改则部分红冲第二次登记在 PRD 层就被堵死**）；P-29 PRD:2421-2422（两行「见本节末尾的规格未定义项」指向 F-08，随本条关闭）；P-30 PRD:2430 比例回滚（**作废分支一律取 issued_ratio − rolled_back_ratio，不得代入含红字金额的公式**，该列在作废行为空）；P-31 PRD:2441/:2444 及新增两行异常（三码语义不重叠：SOURCE_ALREADY_REVERSED 只在状态属已作废或已全额红冲时触发；RED_AMOUNT_MISMATCH 只判超额）；P-32 PRD:2411；P-33 PRD:2464（进项方向，**本轮实测「只允许冲回一次」共四处：2393、2411、2441、2464，多于裁定连带段点名的两处**；本处措辞须与「进项方向是否存在作废」那一刀的结论一致，**不得抢跑**）；P-34 PRD:2884（F-08，体例同 F-14）；P-35 PRD:4505（U-D-09 状态列改「已关闭」）。
- B-8 PRD 侧：P-36 PRD:2356 之后新增「发票行明细」字段行与说明段（行号、关联销售订单与订单行、数量、三项金额；头表金额恒等于行明细合计）；P-37 PRD:2424 之后新增「红冲行明细」字段行（每行指向原发票一条行明细，本次红冲数量不超过该行数量减已红冲数量）。
- **P-38★／P-39★（待裁）**：PRD:2878（F-02）与 PRD:4500（U-D-04）逐字仍含「一张发票是否允许多税率与多行明细」且状态「待决」，而计划10 第 0.4 节临时取值逐字「一张发票单税率、单行金额，不做多行明细」**当场作废**。建议 F-02 拆为两半（多行明细已裁为允许，税率集合仍待决）。B-8 动到未列 PRD 第 6.16 节与附录乙，**属核查发现的连带漏列，工单不代裁**。

#### 批次 3-E：B-1（更正凭证）+ B-2（手工凭证排除）**同批**

详本:505 逐字「B-1 更正凭证落阶段 9b（其守卫要重跑触及的勾稽项，须阶段 10 的子账侧全部接入，故必须排在 B-5/B-6/B-7 之后）→ B-2 与 B-1 同批（同一条决策的两半，不得先落一半）。」

- **S-10｜规格:361 之后新增「更正凭证」条（B-1）**：由财务会计在总账内发起，只用于有来源凭证的重分类更正，不经事件-分录表映射；**每张更正凭证必须且只能引用一张同法人的已过账凭证**；对同一被引用凭证的累计更正借方合计不得超过该凭证借方合计，按被引用凭证逐张独立计算不做链式回溯；借贷合计相等；**同一事务内只重跑本次分录所触及科目角色对应的勾稽项，判据为不劣化，任一项劣化整笔拒绝**，全局十项由每日对账与关账前校验兜底；期间落已关闭期间时顺延；属高风险操作中的财务过账一类。
  三条纪律：不得写入期初余额批次来源；**不得借机动规格:1050「六类」口径**；规格:187／361／1444／1657／1710 五处既有承接锚**不改字**。
- **S-11｜规格:361 之后新增「凭证来源封闭」条（B-2）**：「首版总账凭证只由三个来源产生——十类业务事件的固定映射、期末处理动作（年度损益结转）、更正凭证；不提供手工凭证录入或任何自由分录入口。」
  **措辞纪律**：只写「不提供入口」这一层，**不得附加「因此不存在无业务来源的凭证」一类判断**（该判断已被判失真：更正凭证不查映射表、分录行由入参给定，守卫只保证有来源不保证语义正当）。
- **S-12（建议）｜规格:372「首版不含」枚举**追加「手工凭证录入入口」。若使用方只要一处，以 S-11 为准，本项可不做。
- **P-40｜PRD:2977（B-1 + B-2 合改同一句）**：删末句「首版是否提供手工凭证录入入口，以及更正凭证由谁在何处发起，见 openIssues。」，改为「更正凭证由财务会计在本节的更正凭证入口发起，必须引用一张同法人的已过账凭证，用途限于有来源凭证的重分类更正，须重新认证并经审批，口径按规格第 5.2 章总账功能与期末处理块；**首版不提供手工凭证录入入口**；总账凭证只由十类业务事件的固定映射、年度损益结转与更正凭证三个来源产生。」**两条各改半句会互相覆盖。**
- **P-41｜PRD:2988** 末句后补一句（入口在桌面端与服务端可写、移动端只查看；一张只能引用一张；累计不超来源凭证借方合计；触及勾稽项不劣化，劣化整笔拒绝）。
- **P-42｜PRD:4578（U-H-07）** 状态列改「已关闭，见第 7.3 节更正凭证段」。
- **P-43｜PRD:4579（U-H-08）** 状态列改「已关闭，见规格第 5.2 章总账功能与期末处理块与本文件第 7.3 节」。可在第三列补一句：该行逐字给的不开代价「无入口则期初调整与更正无路径」已不成立。
- **P-44（建议）｜PRD:3228 第 7.10 节「首版不含」**新增一条手工凭证录入入口。
- **P-45（工单自提，须确认是否采纳）｜PRD:2990 之前新增「### 7.4 更正凭证」功能节**并把其后各节顺延编号。理由：2977／2988 两句只能给结论，给不出可实现的字段与异常口径，而 PRD 是首版功能说明书的唯一承接面。**F-10 动到未列此项。** 不采纳则只落 P-40／P-41。

---

### 第四股 · C 簇（C-3 → C-1 → C-2 → C-4）

详本:507 逐字「C-3 默认审批链先落（A-6a 的 TERMINATION 链、B-1 更正凭证的链、B-3 冲正的链**三处取值的唯一出处**，不先落这一条，三处会各给一套）」。

- **P-46｜PRD:4442（U-A-08）**：状态列由「待决」改为已关闭并注明关闭方式；事项列在「到款与付款与退款登记」处加括注「**到款登记不在本裁定的默认链清单内**，是否需审批由 `EP__FINANCE__RECEIPT__REQUIRES_APPROVAL`（默认 false）承载」；后果列末补关闭要点——审批链落 `approval_chains` 与 `approval_chain_nodes`；解析口径为该法人下 `is_active` 为真的**最大 version_no**；**无链或链节点展开为空一律拒绝提交（fail-closed，不开降级窗口）**，但该规则**只作用于本裁定给出默认链的 scenario，不扩到规格:1041/1050 六类高风险操作全集**；角色码取大写形态；具体 code 取值由阶段 4 与阶段 6 同批补齐。
- **P-47（核对动作，不核对不得关闭 U-A-08）｜PRD:4526（U-E-07）、:4533（U-E-14）、:4545（U-F-05）**：三行逐字含「与 U-A-08 关联」，须核一次其立论前提在 U-A-08 转已关闭后是否仍成立；不在 C-3 覆盖面内的，须在各自事项列注明「不在 C-3 默认链覆盖面内，仍待决」。
- **S-13｜规格:920 之后新增「补偿动作策略」条（C-1）**：按法人、环节码、动作码配置是否允许、由哪条审批链批准、是否要求重新认证与理由与附件；承载表主键与**九列封闭**、出厂基线 13 行封闭；经既有配置发布通道下发，**不新增内容项类别，也不新增自动测试套件**。
- **S-14｜规格:926 之后新增（C-1 + C-2 合并一段）**：
  C-1 半段——补偿策略**只有减法与加码**：只能把允许改为不允许、把审批链换成节点更多的链、把三个 requires_* 由假改真；该表上**不提供任何表达事件到分录映射、是否生成凭证、是否写台账、会计期间、唯一约束开关或跳过语义的字段**；单调收紧由纯函数在**配置保存时与运行期提交时各判一次**；**编译期常量只作比较基准，不作运行期权威，表是运行期权威**。
  C-2 半段——**三支不可配定义域**：触发过账触发事件登记表十三行任一事件者、写仅追加登记表十四张表者、**写应收/应付/预收/预付/超量开票五张往来台账者**（第三支不可省：这五张已从仅追加登记表删除，缺此支则核销、部分核销、超量开票挂账变更会**反被判成可配面**）；判定落配置保存期与运行期两道，**不落构建期源码树自检**。
- **S-15｜规格:921 之后新增（C-2）**：状态迁移集合编译期冻结；可配的只有不改变迁移集合、不改变分录与台账写入的前置条件阈值参数，形态限数值或布尔；可配参数清单首版封闭为一张登记表，新增须另裁。
- **P-48｜PRD:2965**：「映射本身不可由客户改写」**一字不动**；其后「可配置的只有每个科目角色绑定到哪个具体科目」改为两类（加第 10.4.4 节补偿动作策略，且只能收紧）。
- **P-49｜PRD:4036**：「不得改动财务模块的事件到分录映射」**一字不动**，句尾追加边界说明（补偿策略表上不存在表达映射／凭证／台账／期间／唯一约束的任何字段，故不构成例外）。
- **P-50｜PRD:4051（C-1 + C-2 合并追加）**：补偿动作策略可配面与 13 行出厂基线；前置条件阈值参数可配，首版已登记一项 `EP__INVOICE__VOID__MAX_DAYS_AFTER_ISSUE`，**并写明「ISSUED → VOIDED 这条迁移本身不可配」**。基线中「已过账凭证的更正凭证」一行取值**以 B-1 为准**，两条不得给出相反取值。
- **P-51｜PRD:4052（C-1 + C-2 合并追加）**：补偿策略不得放宽（凭证改删与期间重开两行恒为不允许，后者因规格:366 逐字「首版不做反结账」）；不得通过任何配置新增、删除或改写状态迁移本身。
- **P-52｜PRD:4015 之后新增一段（C-2）**：可配阈值参数清单封闭为登记表、行集与编译期常量逐字一致；**并写明本项载体形态卡在一处未决矛盾上（现有 EP__ 业务参数启动时读取、变更需重启，与技术基线「运行期可变的业务参数不进配置文件」相反），该矛盾未解之前本项不得排期实现**。此条是 C-2 不能单独落地的前提，须与该未决项一并交裁。
- **C-4**：动到逐字全在阶段 13／10／4，规格与 PRD 无落点。实测规格:577 原文未动，方向已一致。唯一文档动作是在阶段 4 计划写明「该段用例期望错误码经本条核过、不需改」，**不在本工单范围**。

---

### 第五股 · D 簇（可全程并行；D-3 必须最后）

详本:508 逐字「D-3 表述权限与诚实披露**必须最后**：它新增的三条披露内容直接取自 A-3/A-12、B-1/B-2、D-1 三处裁定，那三处定稿之前 D-3 无法定稿。」

- **S-16｜规格:1197 之后、:1199 之前新增一条项目符号（D-1）**：把备份保留期取值与其治理显式列为 13.3 可用性目标的前提，并回指 13.4。**逐字文本详本未给，须使用方定稿**（见第 5 节诚实标注）。
- **S-17｜规格:1206（D-1 治理半）**：现原文已由 commit b7f2687 写入取值（逐字「- 备份保留期认证取值为 14 天，同时要求任一时点在保留期内至少存在两代校验通过的有效全量备份，两个条件取严。……」），**治理一半未写**。补：(a) 保留期为单一取值，不按对象类型、模块或单据类型分列；(b) 变更须**双人审批加重新认证**，留 approval_ref、approver_id、second_approver_id、reauth_ref 四项证据；(c) 以 effective_from 与 superseded_at 表达版本，历史行不覆盖；(d) **下限不低于 7 天，取值超过 14 天须有该部署非空的演练报告引用，上限不写进落库 CHECK**。落库形态属阶段 14，本处只写规范性要求。
- **S-18｜规格:1717（第 22 章第 13 条）追加半句（D-1）**：恢复文档须载明当前生效保留期取值、超出保留期不可恢复这一后果、以及四项证据留存位置。**逐字文本详本未给。** 原 D-12-1 挂在第 22 章第 7 条上的判据已因恒真被打掉，**不得复活到第 7 条**。
- **S-19★（D-2，落地前置冲突，须先裁）**：D-2 把 basis 定义为「GENERATIONS_SHORT（有效全量代数低于 3）」，与规格:1206 已落盘的「至少存在两代」**直接冲突（2 对 3）**。二选一：改规格:1206 为「三代」（同批须核该行句末「不再是最后两代之一的备份集方可销毁」与 D-1 落库列 `min_valid_generations`），或把阈值改为「低于 2」。**该冲突在 00c 与 00d 均零命中，尚未登记。工单不代裁。**
- **S-20｜规格:1700 处新增「### 21.22 对外表述权限与诚实披露」（D-3）**。本轮实测规格全文「21.22」「表述权限」「碾压」均零命中，属新增。四项必含：三档划分（第一档已认证事实无条件用；第二档比较级须实测举证；第三档禁「碾压」「行业模板」「实施顾问」「生态伙伴」四词及同类词作承诺性表述与一切未经实测的比较级）；三档裁决权归产品负责人、法务前置结论为第二档必要条件；**第二档举证口径本轮不定，定下来之前第二档表述一律不得使用**（净效果：不得对外宣称任何比较级优势）；**本节显式排除在计划14:568 文本检查对象之外**（否则两条裁定落地即互相判违反）。
- **S-21｜规格:1599（21.4 签字对象扩面，D-3）**：现原文逐字含「会计与税务、法务、安全三类按下列高风险门槛签字」，新增第四类「对外表述与宣传材料」，裁决与签字方为产品负责人。**该类的阶段归属详本未给，须使用方定稿。**
- **P-53｜PRD:4323（D-3）**：「以下八条」改为「以下十一条」。三处计数须逐字一致：本节小节数、计划14 退出条件第 16 项的计数、客户合同模板条目数。**注意该退出条件项在计划 14 的实际行号是 45，不是详本两处误写的 44。**
- **P-54｜PRD:4376 处新增 11.11.9 / 11.11.10 / 11.11.11（D-3）**。实测现有八小节标题依次在 PRD:4325、4329、4338、4352、4356、4360、4369、4373，:4377 为「### 11.12」，第九小节确为新增。三条内容分别取自 A-3/A-12（终止处置期间不能开票、发货、改单；闭合时长取决于人工决策速度）、B-1/B-2（首版不做反结账、不提供手工凭证入口、更正凭证必须引用一张原凭证）、D-1（保留期 14 天、超出不可恢复、恢复点粒度受落点形态影响）。**那三处若在入卷时被改，本条须同批改**；三条新文本还须在阶段 13 的产品界面可达处呈现。
- 一处**需说明但不构成违反**：「行业模板」在规格命中 2 处、PRD 命中 5 处，全部是「不预置／未收录」的否定陈述或待决项标题，**不是承诺性表述**；且文本检查对象仅限交付、认证与验收材料，规格与 PRD 本就不在检查对象内。**回写时不得把检查面扩到规格与 PRD**——那正是已撤下的 D-13-3。

---

## 4. 风险与陷阱

**风险一：照 F-10 原文机械回写＝把已被「必改」否决的旧版本写进最高权威文档。**
27 条里 25 条被自查缺陷表修正过（只有 A-5 零命中、A-6a 本身即修正产物）。裁定正文与其草案在同一份文件里并存，逐字引用时极易引到被否决的那一版。**高危十例**（回写文本中出现即为错）：

| 不得写 | 应写 |
|---|---|
| WAIVED 状态、approval_ref | state=DONE 加非空 `decision_reason` |
| ImpactRegistry 注册项数恰为七 | 目录条数恒为七（编译期常量）；注册数按阶段 3/4/6/7 |
| 未注册规则其处置项不产出 | 一律建占位项，state 恒 PENDING、不计入 item_done |
| 覆盖面含规格第 8 章第 13 步 | 第 13 步已过账凭证**显式摘出**并写明理由 |
| A-7 MANUAL_DECISION「三条」分支 | **四条**（第 3、7 恒为该类 + 第 5 条 ORDERED + 第 6 条 IN_PROGRESS） |
| A-12「九条断言」 | **十条断言**（详本:67 已改，后续引用一律按十） |
| B-1 行级引用 + 行级累计上限、重跑全局十项勾稽 | 凭证级引用 + 凭证借方合计上限；只重跑触及项且判据为不劣化 |
| B-6 只在附加规则栏加一句 | **必须改借方栏与贷方栏**，附加规则栏只是从属说明 |
| C-1 的 SHA-256 冻结机制 | 编译期常量只作比较基准，表是运行期权威 |
| C-3 fail-open 挂 DegradationLedger、作用于六类全集 | fail-closed；**只作用于给出默认链的 scenario** |
| A-8 期次 VOIDED 的管辖被写成「本卷内」 | 管辖面**只限合同对象**；发票/订单/期次的作废各按自身状态机 |
| B-4 作废分支代入含红字金额的回滚公式 | 作废一律取 issued_ratio − rolled_back_ratio |

**风险二：五处锚点在现文档里根本不存在，属「新增节」而非改写。**
本轮逐一实测确认：

| 落点 | 实测现状 |
|---|---|
| PRD 3.5.5 影响面处置台账 | 全文 `3.5.5` 零命中；3.5 下最末子节是 :1046 的 3.5.4，其后直接 :1058 的 3.6 |
| 规格第 5.2 章「资金单据冲正」块 | 规格全文「冲正」**零命中** |
| 规格第 21.22 节 | 「21.22」「表述权限」「碾压」全零命中 |
| PRD 11.11.9 / .10 / .11 | 11.11 现只有 .1 至 .8（:4325…:4373） |
| PRD 6.4 / 6.5 的发票行明细字段 | 6.4-6.5 两节字段表内「行明细／发票行／订单行／数量」**零命中** |
| PRD 7.4 更正凭证功能节（工单自提） | :2990 现直接是「### 7.4 账表查询与试算平衡」 |

新增节意味着：编号顺延、交叉引用面、目录与节号映射表（U-A-13 已关闭项即此类）都要同批核。

**风险三：附录乙状态列取值。** PRD:4429 逐字只许「待决」与「已关闭」两值，而 F-10 多条动到写「改已裁定／改已决」。**照抄裁定用词会当场违反 PRD 自己的体例**（全表 167 行中 165 行「待决」、2 行「已关闭，见…」）。

**风险四：第 6.16 节没有状态列。** 表头 PRD:2875 三列、节前言 PRD:2873 逐字「本节不自行取值」。F-08、F-14 等条目的关闭方式只有删行或在描述列追加括注两种，**须一次定好体例**（本轮至少 F-02、F-08、F-14 三条受影响）。

**风险五：拆批即在规格上留下自相矛盾的半句。** 详本代价合计段逐字：「B-4 与 B-8 必须同批、B-3 与 B-6 必须同批、B-1 与 B-2 必须同批，三组各自构成一个不可拆的交付批次，任何一组拆开都会在规格上留下自相矛盾的半句。」同理 A-5 不落，A-1/A-3/A-4 的 3.5.5 与词义段全部引空。

**风险六：计数。** 详本自陈「本卷已因凭记忆复述计数栽过三次，这是第四个高危点」。本轮历史回写曾至少牵动：PRD 11.11「八条→十一条」（三处须一致）、`docs/` 登记文件「四份→五份」（且 `00-overview.md:323` 与 `01-engineering-baseline.md:548` 列的四份**本就不是同一套**，前者含「配置参考」无「数据字典」，改「五份」时会把既有矛盾拉大）、阶段 6 事件总数「18 还是 20」（**本轮不代数，须回去把九个未命名名额逐条数清**）以及四份阶段计划退出条件条数。D-12-4 的旧“阶段 14 十八改十九”已撤销；F-52 曾冻结十九项，F-53 当时追加病毒扫描项形成 20 项。该历史口径已被 F-55 的“阶段 2 首次三项、Stage 14a 扩 Rust contract、`V20261023092500` 扩数据库 CHECK 为终态 21 项”取代，本句不得实施。

**风险七：两处卷内对撞须同批清理。** 其一 00c 第 2792 行一带仍写「`WAIVED` 加 `approval_ref`」；其二 规格:1206「两代」与 D-2「低于 3」（2 对 3），**后者在 00c 与 00d 均未登记**。

---

## 5. 诚实标注：未能定位到确切锚点 / 尚缺逐字文本的条目

以下各项**不具备直接执行条件**，须使用方补齐后方可入卷：

1. **D-1 · 规格 13.3 新增句**：落点区间已定（:1197 之后、:1199 之前），但**逐字文本详本未给**。原动到写「13.3 新增一句（保留期取值与其治理）」，而 b7f2687 已把「取值」那半写进 13.4:1206，13.3 这一句只余「前提声明与回指」，措辞待定稿。
2. **D-1 · 规格第 22 章第 13 条追加半句**：锚点确凿（:1717 实测在位），**逐字文本详本未给**，第 3 节所列为本工单提出的可落地措辞。
3. **D-3 · 规格 21.4 新增签字类的阶段归属**：现行三类归属逐字为「会计与税务、法务在其所属模块的建设阶段签字，安全在发布前的硬化阶段签字」，**新增类归属详本未给**。
4. **D-3 · 规格 21.22 三档的第二档实测举证口径**：详本明写随已撤下的 D-13-1 另裁，**本工单不得代补**；在口径定下来之前该档表述一律禁用。
5. **A-5 · PRD 3.6 状态机是三行新增还是四行**：动到字面为「新增四行」，但按四行会同时留下两条互斥边。本工单按三行新增 + 两行改写，**须拍板**。
6. **A-9 · 阶段 6 事件总数 18 还是 20**：详本逐字「本裁定不代数」，且已升为 still_open 第 5 条。**A-5 新增的四条迁移边的事件名归属仍悬。**
7. **A-3 · `clm.contract_termination_disposition` 的 `max_instance_duration_days`**：登记为未决，**不得在 PRD 里填数字**。
8. **A-8 · PRD 第 3.9.2 节与 PRD:4100 的提醒排除口径**：A-8 动到未列 PRD，但 PRD 现文逐字承诺按全部期次与全部交付节点发提醒，与阶段 6 视图新语义不一致。**是否纳入，须拍板。**
9. **B-1 · 新增 PRD「### 7.4 更正凭证」功能节**：F-10 动到未列此项，属工单自提；不采纳则更正凭证在 PRD 侧只有两句结论、无字段与异常口径。
10. **B-2 · 规格:372 与 PRD:3228 的「首版不含」两处加句**：动到未点名，属工单建议（理由：排除条款只出现在正文一句时检索面太窄）。
11. **B-7 · 规格:1352 与 PRD:2787-2790**：裁定明写「规格第 10.2 章不改、PRD 6.13.2 不改」，但**未提** 17.3 章与 6.13.1；这两处现文正是被 B-7 判死的当前值口径，不改则规格判定文本与实现相反。**核查登记，非裁定要求。**
12. **B-8 · PRD:2878（F-02）与 PRD:4500（U-D-04）／PRD:2882（F-06）与 PRD:4504（U-D-08）**：动到只列阶段 10 第 0.4 节，未列 PRD 同源待决登记；不同批处置会出现「计划已定、PRD 仍待决」的反向落差。**核查登记，非裁定要求。**
13. **D-2 · GENERATIONS_SHORT 阈值与规格:1206「两代」的冲突**：二选一，**工单不代裁**。
14. **计划侧行号**：本轮只抽核 `计划06:612`、`计划06:913` 两处在位（前者逐字「本阶段的事件总数固定为 18，第 1 节与第 9 节的计数只引用本小节，不另写数字。其中九个……」，后者逐字「#### 11.5 跨阶段调用点的接线次序」）。**其余计划侧引用（计划07:487、计划09:518、计划10:341/489/691/786/802、计划12:887、计划13:263/444、计划14:335/568 等）沿用详本原文，本轮未复算**，入卷时须当场 sed 验。
15. **A-2 连带发现、裁定未覆盖**：`00-overview.md:323` 逐字列「错误码表、事件目录、指标目录、数据字典」四份，`01-engineering-baseline.md:548` 逐字列「错误码表、事件目录、指标目录、配置参考」四份——**两处本就不是同一套**。加 `docs/impact-catalog.md` 改「五份」时须一并核对，否则把既有矛盾拉得更大。**建议使用方一并裁。**
