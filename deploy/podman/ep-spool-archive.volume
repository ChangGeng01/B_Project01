# ep-spool-archive：archive-writer 在 core-server 不可用期间的本地暂存，配置键 spool.dir。
# VolumeName= 固定卷名，不取 Quadlet 默认的 systemd- 前缀；Compose 一侧同名 volume 写 name: ep-spool-archive，
# 两套据此落在同一个卷上，等价性核对脚本比对的就是这个名字。需 Podman 5.0 及以上。
[Volume]
VolumeName=ep-spool-archive

[Install]
WantedBy=multi-user.target
