# 规格第 13.1 章配额表第 3 行「Worker」，CPU 10%、内存 8%、磁盘 IO 8%。
[Slice]
# 与 app-core.slice 同为搜索索引负载的实际承载方，欠配情形同该 slice，见 README 的偏差一。
CPUWeight=1000
IOWeight=800
# 内部对账批次在本份额之内执行，其相对于 Worker 内其余后台任务的优先由进程自身调度落实，
# cgroup 不在一个 slice 内部区分优先级，本文件因此不表达该次序。
MemoryLow=5221908480
MemoryMax=5221908480
