# 规格第 13.1 章配额表第 2 行「Rust 核心与集成网关」，CPU 20%、内存 16%、磁盘 IO 11%。
# core-server 与 integration-gateway 是两个进程但同处本 slice，slice 内不再细分配额。
[Slice]
# 本 slice 与 app-worker.slice 另承载配额表第 4 行「内置搜索索引」的实际负载，
# 而该行不落 drop-in、不加和、不拆分，因此这两个 slice 相对规格意图欠配，见 README 的偏差一。
CPUWeight=2000
IOWeight=1100
# 交易路径上的附件正文读写由本 slice 发起并计入本行的磁盘 IO 份额。
MemoryLow=10444865536
MemoryMax=10444865536
