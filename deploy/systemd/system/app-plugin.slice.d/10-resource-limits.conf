# F-57：HISTORICAL_LINUX_RESEARCH_ONLY；禁止用于当前 Windows Server 2022 生产部署或发布验收。
# 规格第 13.1 章配额表第 5 行「插件运行时」，CPU 5%、内存 5%、磁盘 IO 4%。
[Slice]
# 本 slice 是机器级外壳，单插件的燃料、内存、实例数与执行时限由 plugin-host 自身承担；
# 两套不并存为两个闸门，本文件只给外壳，不表达单插件限额。
CPUWeight=500
IOWeight=400
MemoryLow=3263168512
MemoryMax=3263168512
