# 规格第 13.1 章配额表第 7 行「公网门户（portal-gateway）」，CPU 3%、内存 3%、磁盘 IO 1%。
[Slice]
# 门户单列一行而不与反向代理共用 cgroup，是因为共用会取消独立进程与独立系统账户这条边界。
# 但两个权重列只在竞争时约束份额：其余 slice 空闲时被攻破或遭洪泛的门户仍可借用整机空闲量，
# 规格该章突发上限一列在首版无承载，资源侧遏制因此只覆盖内存一维与两个权重份额，见 README 的偏差二。
CPUWeight=300
IOWeight=100
MemoryLow=1957691392
MemoryMax=1957691392
