# F-57：HISTORICAL_LINUX_RESEARCH_ONLY；禁止用于当前 Windows Server 2022 生产部署或发布验收。
# app-archive.slice：规格第 13.1 章配额表第 8 行「连续归档、附件增量与审计证据写出」的 cgroup 承载物，承载进程 archive-writer。
# 本单元不写任何配额取值。四类取值一律只落在 app-archive.slice.d/10-resource-limits.conf 一处，
# 单元里再写一份就是第二套取值，改一处漏一处时无从判定哪一处为准。

[Unit]
Description=EP 单机部署：连续归档写出分片
Before=slices.target
